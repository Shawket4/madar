/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// PriceRoundingRule : Serializes as `\"EgyptianCafe\"` / `\"NearestUnit\"` — PascalCase on the wire (no `rename_all`); existing clients depend on it.
/// Serializes as `\"EgyptianCafe\"` / `\"NearestUnit\"` — PascalCase on the wire (no `rename_all`); existing clients depend on it.
#[derive(Clone, Copy, Debug, Eq, PartialEq, Ord, PartialOrd, Hash, Serialize, Deserialize)]
pub enum PriceRoundingRule {
    #[serde(rename = "EgyptianCafe")]
    EgyptianCafe,
    #[serde(rename = "NearestUnit")]
    NearestUnit,

}

impl std::fmt::Display for PriceRoundingRule {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        match self {
            Self::EgyptianCafe => write!(f, "EgyptianCafe"),
            Self::NearestUnit => write!(f, "NearestUnit"),
        }
    }
}

impl Default for PriceRoundingRule {
    fn default() -> PriceRoundingRule {
        Self::EgyptianCafe
    }
}

