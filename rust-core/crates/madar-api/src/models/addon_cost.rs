/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// AddonCost : Computed cost for one addon item.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct AddonCost {
    #[serde(rename = "addon_item_id")]
    pub addon_item_id: uuid::Uuid,
    #[serde(rename = "addon_type")]
    pub addon_type: String,
    /// Ingredient cost rollup in piastres over the ingredients that *are* priced. A partial rollup still returns the sum so far, with `cost_missing = true`; `null` only when nothing is priced.
    #[serde(rename = "cost", default, with = "::serde_with::rust::double_option", skip_serializing_if = "Option::is_none")]
    pub cost: Option<Option<i64>>,
    /// `true` when at least one ingredient is unlinked or has no cost, so `cost` (if any) is partial rather than the full figure.
    #[serde(rename = "cost_missing")]
    pub cost_missing: bool,
    /// `(price - cost) / price` — only when the cost is *complete* and price > 0.
    #[serde(rename = "margin_pct", default, with = "::serde_with::rust::double_option", skip_serializing_if = "Option::is_none")]
    pub margin_pct: Option<Option<f64>>,
    #[serde(rename = "name")]
    pub name: String,
    /// Default price in piastres.
    #[serde(rename = "price")]
    pub price: i64,
}

impl AddonCost {
    /// Computed cost for one addon item.
    pub fn new(addon_item_id: uuid::Uuid, addon_type: String, cost_missing: bool, name: String, price: i64) -> AddonCost {
        AddonCost {
            addon_item_id,
            addon_type,
            cost: None,
            cost_missing,
            margin_pct: None,
            name,
            price,
        }
    }
}

