/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// TableOrderRequest : One scan's worth of order.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct TableOrderRequest {
    /// Who is at the table, if they offered a name. Shown on the bill so the waiter can find them.
    #[serde(
        rename = "customer_name",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub customer_name: Option<Option<String>>,
    /// Client-minted, so a phone that resends on a flaky connection does not order twice. This is the ONLY protection against a double-send, because a customer's browser has no outbox to dedup against.
    #[serde(
        rename = "idempotency_key",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub idempotency_key: Option<Option<uuid::Uuid>>,
    /// What they want. Named, never priced — see the module docs.
    #[serde(rename = "items")]
    pub items: Vec<models::OrderItemInput>,
    #[serde(rename = "table_id")]
    pub table_id: uuid::Uuid,
}

impl TableOrderRequest {
    /// One scan's worth of order.
    pub fn new(items: Vec<models::OrderItemInput>, table_id: uuid::Uuid) -> TableOrderRequest {
        TableOrderRequest {
            customer_name: None,
            idempotency_key: None,
            items,
            table_id,
        }
    }
}
