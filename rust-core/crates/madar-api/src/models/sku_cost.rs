/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// SkuCost : Computed cost for one sellable SKU (menu item × size).
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct SkuCost {
    #[serde(rename = "category_id", default, with = "::serde_with::rust::double_option", skip_serializing_if = "Option::is_none")]
    pub category_id: Option<Option<uuid::Uuid>>,
    /// Recipe cost rollup in piastres over the ingredients that *are* priced. `null` only when there is no recipe, or no recipe ingredient has a known cost at all. A partial rollup (some ingredients unpriced) still returns the sum so far, with `cost_missing = true` flagging it as incomplete.
    #[serde(rename = "cost", default, with = "::serde_with::rust::double_option", skip_serializing_if = "Option::is_none")]
    pub cost: Option<Option<i64>>,
    /// `true` when at least one recipe ingredient is unlinked or has no cost, so `cost` (if any) is a partial figure rather than the full COGS.
    #[serde(rename = "cost_missing")]
    pub cost_missing: bool,
    /// `cost / price` — only when the cost is *complete* and price > 0.
    #[serde(rename = "food_cost_pct", default, with = "::serde_with::rust::double_option", skip_serializing_if = "Option::is_none")]
    pub food_cost_pct: Option<Option<f64>>,
    #[serde(rename = "item_name")]
    pub item_name: String,
    /// `(price - cost) / price` — only when the cost is *complete* and price > 0. Suppressed (`null`) for partial rollups so an incomplete cost is never graded as a food-cost percentage.
    #[serde(rename = "margin_pct", default, with = "::serde_with::rust::double_option", skip_serializing_if = "Option::is_none")]
    pub margin_pct: Option<Option<f64>>,
    #[serde(rename = "menu_item_id")]
    pub menu_item_id: uuid::Uuid,
    /// Current price in piastres for this SKU.
    #[serde(rename = "price")]
    pub price: i64,
    /// `\"one_size\"` when the item has no sizes.
    #[serde(rename = "size_label")]
    pub size_label: String,
}

impl SkuCost {
    /// Computed cost for one sellable SKU (menu item × size).
    pub fn new(cost_missing: bool, item_name: String, menu_item_id: uuid::Uuid, price: i64, size_label: String) -> SkuCost {
        SkuCost {
            category_id: None,
            cost: None,
            cost_missing,
            food_cost_pct: None,
            item_name,
            margin_pct: None,
            menu_item_id,
            price,
            size_label,
        }
    }
}

