/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// OnboardingStep : One derived setup step.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct OnboardingStep {
    /// Supporting count (branches created, items added, …).
    #[serde(rename = "count")]
    pub count: i64,
    /// True when the underlying data exists.
    #[serde(rename = "done")]
    pub done: bool,
    /// Stable key the dashboard switches on — never localized.
    #[serde(rename = "key")]
    pub key: String,
    /// Steps that are encouraged but not blocking (`required = false` never gates `can_complete`).
    #[serde(rename = "required")]
    pub required: bool,
}

impl OnboardingStep {
    /// One derived setup step.
    pub fn new(count: i64, done: bool, key: String, required: bool) -> OnboardingStep {
        OnboardingStep {
            count,
            done,
            key,
            required,
        }
    }
}

