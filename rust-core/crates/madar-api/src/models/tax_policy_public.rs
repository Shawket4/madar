/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// TaxPolicyPublic : The tax policy the caller should price under, resolved for their branch where they have one and their organisation otherwise.  Sent at login and on every `/auth/me`, which is what makes a rate change reach a till that has been running for weeks. The flat `tax_rate` beside it is kept for builds that predate this object; both describe the same rate.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct TaxPolicyPublic {
    /// Fraction of the bill added as a service charge; `0` disables it.
    #[serde(rename = "service_charge_rate")]
    pub service_charge_rate: f64,
    /// Whether the service charge is itself taxed.
    #[serde(rename = "service_charge_taxable")]
    pub service_charge_taxable: bool,
    /// `true` = menu prices already contain the tax.
    #[serde(rename = "tax_inclusive")]
    pub tax_inclusive: bool,
    /// Fraction, NOT a percentage: `0.14` is 14%.
    #[serde(rename = "tax_rate")]
    pub tax_rate: f64,
}

impl TaxPolicyPublic {
    /// The tax policy the caller should price under, resolved for their branch where they have one and their organisation otherwise.  Sent at login and on every `/auth/me`, which is what makes a rate change reach a till that has been running for weeks. The flat `tax_rate` beside it is kept for builds that predate this object; both describe the same rate.
    pub fn new(service_charge_rate: f64, service_charge_taxable: bool, tax_inclusive: bool, tax_rate: f64) -> TaxPolicyPublic {
        TaxPolicyPublic {
            service_charge_rate,
            service_charge_taxable,
            tax_inclusive,
            tax_rate,
        }
    }
}

