/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// ElsewhereShift : A person's shift at ANOTHER branch that date (BUG-4): the board shows it (\"at <branch>\") so the cell never reads \"Off\", but it is not this board's: never one of `shifts`, never sent back by a PUT from here.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct ElsewhereShift {
    #[serde(rename = "branch_id")]
    pub branch_id: uuid::Uuid,
    #[serde(rename = "branch_name")]
    pub branch_name: String,
    #[serde(rename = "crosses_midnight")]
    pub crosses_midnight: bool,
    #[serde(rename = "date")]
    pub date: chrono::NaiveDate,
    #[serde(rename = "employee_id")]
    pub employee_id: uuid::Uuid,
    #[serde(rename = "end_time")]
    pub end_time: String,
    #[serde(rename = "shift_name")]
    pub shift_name: String,
    #[serde(rename = "start_time")]
    pub start_time: String,
    #[serde(rename = "work_shift_id")]
    pub work_shift_id: uuid::Uuid,
}

impl ElsewhereShift {
    /// A person's shift at ANOTHER branch that date (BUG-4): the board shows it (\"at <branch>\") so the cell never reads \"Off\", but it is not this board's: never one of `shifts`, never sent back by a PUT from here.
    pub fn new(
        branch_id: uuid::Uuid,
        branch_name: String,
        crosses_midnight: bool,
        date: chrono::NaiveDate,
        employee_id: uuid::Uuid,
        end_time: String,
        shift_name: String,
        start_time: String,
        work_shift_id: uuid::Uuid,
    ) -> ElsewhereShift {
        ElsewhereShift {
            branch_id,
            branch_name,
            crosses_midnight,
            date,
            employee_id,
            end_time,
            shift_name,
            start_time,
            work_shift_id,
        }
    }
}
