/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// Grain : The *shape* of a result set, derived from the dimensions a query groups by. This is what lets a dashboard render any metric with no per-metric code: a scalar becomes a KPI card, a series becomes a line, a breakdown becomes a bar or pie.
/// The *shape* of a result set, derived from the dimensions a query groups by. This is what lets a dashboard render any metric with no per-metric code: a scalar becomes a KPI card, a series becomes a line, a breakdown becomes a bar or pie.
#[derive(Clone, Copy, Debug, Eq, PartialEq, Ord, PartialOrd, Hash, Serialize, Deserialize)]
pub enum Grain {
    #[serde(rename = "scalar")]
    Scalar,
    #[serde(rename = "series")]
    Series,
    #[serde(rename = "categorical")]
    Categorical,
    #[serde(rename = "table")]
    Table,

}

impl std::fmt::Display for Grain {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        match self {
            Self::Scalar => write!(f, "scalar"),
            Self::Series => write!(f, "series"),
            Self::Categorical => write!(f, "categorical"),
            Self::Table => write!(f, "table"),
        }
    }
}

impl Default for Grain {
    fn default() -> Grain {
        Self::Scalar
    }
}

