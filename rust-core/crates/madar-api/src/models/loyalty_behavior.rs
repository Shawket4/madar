/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// LoyaltyBehavior : Behavioral rates over the range — how much of the member base actually uses the programme, not just what it's worth. `total_members` and `members_ever_redeemed` are org-wide and lifetime (a balance/history isn't branch-scoped); every other figure narrows to `branch_id` and the range, same as [`LoyaltyAnalytics`].
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct LoyaltyBehavior {
    /// `active_members / total_members`. `0.0` when there are no members.
    #[serde(rename = "active_member_rate")]
    pub active_member_rate: f64,
    /// Distinct (not deleted) members with any loyalty transaction in the range — deleted members are left out of every count so no rate over `total_members` can exceed 1.
    #[serde(rename = "active_members")]
    pub active_members: i64,
    #[serde(rename = "from")]
    pub from: chrono::DateTime<chrono::FixedOffset>,
    /// Distinct (not deleted) members who have ever redeemed a reward. Org-wide, lifetime.
    #[serde(rename = "members_ever_redeemed")]
    pub members_ever_redeemed: i64,
    /// `new_members_active / active_members`.
    #[serde(rename = "new_member_share")]
    pub new_member_share: f64,
    /// Active members who enrolled during the range.
    #[serde(rename = "new_members_active")]
    pub new_members_active: i64,
    /// Members with exactly 1 earning visit in the range.
    #[serde(rename = "one_time_members")]
    pub one_time_members: i64,
    /// `members_ever_redeemed / total_members`.
    #[serde(rename = "redemption_rate")]
    pub redemption_rate: f64,
    /// `redeemed_points_period / earned_points_period` — the share of what was earned in the range that got spent in it. Points earned before the range and redeemed inside it are not the numerator's earn, so this can exceed 1.0 on a range with heavy redemption of an older balance.
    #[serde(rename = "redemption_ratio")]
    pub redemption_ratio: f64,
    /// Members with 2+ earning visits in the range.
    #[serde(rename = "repeat_members")]
    pub repeat_members: i64,
    /// `repeat_members / (repeat_members + one_time_members)`. `0.0` when nobody earned in the range.
    #[serde(rename = "repeat_visit_rate")]
    pub repeat_visit_rate: f64,
    /// Active members who enrolled before the range started.
    #[serde(rename = "returning_members_active")]
    pub returning_members_active: i64,
    #[serde(rename = "to")]
    pub to: chrono::DateTime<chrono::FixedOffset>,
    /// Enrolled, not deleted, as of now. Org-wide.
    #[serde(rename = "total_members")]
    pub total_members: i64,
}

impl LoyaltyBehavior {
    /// Behavioral rates over the range — how much of the member base actually uses the programme, not just what it's worth. `total_members` and `members_ever_redeemed` are org-wide and lifetime (a balance/history isn't branch-scoped); every other figure narrows to `branch_id` and the range, same as [`LoyaltyAnalytics`].
    pub fn new(
        active_member_rate: f64,
        active_members: i64,
        from: chrono::DateTime<chrono::FixedOffset>,
        members_ever_redeemed: i64,
        new_member_share: f64,
        new_members_active: i64,
        one_time_members: i64,
        redemption_rate: f64,
        redemption_ratio: f64,
        repeat_members: i64,
        repeat_visit_rate: f64,
        returning_members_active: i64,
        to: chrono::DateTime<chrono::FixedOffset>,
        total_members: i64,
    ) -> LoyaltyBehavior {
        LoyaltyBehavior {
            active_member_rate,
            active_members,
            from,
            members_ever_redeemed,
            new_member_share,
            new_members_active,
            one_time_members,
            redemption_rate,
            redemption_ratio,
            repeat_members,
            repeat_visit_rate,
            returning_members_active,
            to,
            total_members,
        }
    }
}
