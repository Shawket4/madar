/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// CardView : The member's own card page — what they see when they open the link again.  The token in the path is the member's secret, which is why this returns only what the pass already shows and never the phone number in full.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct CardView {
    /// The live balance, in `mode`'s currency.
    #[serde(rename = "balance")]
    pub balance: i32,
    #[serde(rename = "can_redeem")]
    pub can_redeem: bool,
    #[serde(rename = "member_token")]
    pub member_token: String,
    #[serde(rename = "mode")]
    pub mode: String,
    #[serde(rename = "name")]
    pub name: String,
    #[serde(rename = "next_reward_cost")]
    pub next_reward_cost: i32,
    #[serde(rename = "passes")]
    pub passes: Box<models::PassLinks>,
    #[serde(rename = "points_to_next_reward")]
    pub points_to_next_reward: i32,
    #[serde(rename = "program_name")]
    pub program_name: String,
    #[serde(rename = "rewards")]
    pub rewards: Vec<models::PublicReward>,
}

impl CardView {
    /// The member's own card page — what they see when they open the link again.  The token in the path is the member's secret, which is why this returns only what the pass already shows and never the phone number in full.
    pub fn new(balance: i32, can_redeem: bool, member_token: String, mode: String, name: String, next_reward_cost: i32, passes: models::PassLinks, points_to_next_reward: i32, program_name: String, rewards: Vec<models::PublicReward>) -> CardView {
        CardView {
            balance,
            can_redeem,
            member_token,
            mode,
            name,
            next_reward_cost,
            passes: Box::new(passes),
            points_to_next_reward,
            program_name,
            rewards,
        }
    }
}

