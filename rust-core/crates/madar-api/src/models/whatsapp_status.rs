/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// WhatsappStatus : Snapshot returned to the dashboard. Combines the gateway's live link state with the backend's persisted pause switch.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct WhatsappStatus {
    /// `WHATSAPP_SERVICE_URL` is set on the backend.
    #[serde(rename = "configured")]
    pub configured: bool,
    /// Underlying socket is connected to WhatsApp.
    #[serde(rename = "connected")]
    pub connected: bool,
    /// A pairing QR is currently available to scan.
    #[serde(rename = "has_qr")]
    pub has_qr: bool,
    /// A number is linked and ready to send.
    #[serde(rename = "logged_in")]
    pub logged_in: bool,
    /// Sending is paused by an admin — the number stays linked but every outbound message (OTP + status) is suppressed until resumed.
    #[serde(rename = "paused")]
    pub paused: bool,
    /// When sending was last paused (audit).
    #[serde(rename = "paused_at", default, with = "::serde_with::rust::double_option", skip_serializing_if = "Option::is_none")]
    pub paused_at: Option<Option<chrono::DateTime<chrono::FixedOffset>>>,
    /// Current pairing QR as a `data:image/png;base64,…` URL (only when `has_qr`).
    #[serde(rename = "qr_image", default, with = "::serde_with::rust::double_option", skip_serializing_if = "Option::is_none")]
    pub qr_image: Option<Option<String>>,
    /// The gateway answered over HTTP (false = not configured or unreachable).
    #[serde(rename = "reachable")]
    pub reachable: bool,
    /// Session name the relay pairs/sends under.
    #[serde(rename = "session")]
    pub session: String,
}

impl WhatsappStatus {
    /// Snapshot returned to the dashboard. Combines the gateway's live link state with the backend's persisted pause switch.
    pub fn new(configured: bool, connected: bool, has_qr: bool, logged_in: bool, paused: bool, reachable: bool, session: String) -> WhatsappStatus {
        WhatsappStatus {
            configured,
            connected,
            has_qr,
            logged_in,
            paused,
            paused_at: None,
            qr_image: None,
            reachable,
            session,
        }
    }
}

