/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// TicketBill : What the party owes, priced where the books are priced.  The till used to show the ticket's running `subtotal` and collect that, while the settle booked subtotal − discount + service charge + tax. Every such drawer was short by the tax on every table sale, and the drift check at settle now refuses that figure outright — so the till must be shown the right one, and only the server knows the branch's policy. Priced under the same engine and the same resolved policy as `create_order_inner`, with the service charge on (a ticket is dine-in by definition, ruling 2). For a settled ticket the figures are the ORDER's, as booked, not a repricing under today's policy.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct TicketBill {
    /// The waiter's discount, resolved (a `discount_id` is looked up the way the settle looks it up). A cashier who clears it at settle will see a different total than this one, and that is the point of showing it.
    #[serde(rename = "discount_amount")]
    pub discount_amount: i32,
    #[serde(rename = "service_charge_amount")]
    pub service_charge_amount: i32,
    #[serde(rename = "service_charge_rate")]
    pub service_charge_rate: f64,
    /// Live lines as charged, before discount. Gross when tax-inclusive.
    #[serde(rename = "subtotal")]
    pub subtotal: i32,
    /// Inside the total when `tax_inclusive`, on top of it otherwise.
    #[serde(rename = "tax_amount")]
    pub tax_amount: i32,
    #[serde(rename = "tax_inclusive")]
    pub tax_inclusive: bool,
    /// The rates the figures were computed under, for the printed bill.
    #[serde(rename = "tax_rate")]
    pub tax_rate: f64,
    /// What the drawer must collect.
    #[serde(rename = "total")]
    pub total: i32,
}

impl TicketBill {
    /// What the party owes, priced where the books are priced.  The till used to show the ticket's running `subtotal` and collect that, while the settle booked subtotal − discount + service charge + tax. Every such drawer was short by the tax on every table sale, and the drift check at settle now refuses that figure outright — so the till must be shown the right one, and only the server knows the branch's policy. Priced under the same engine and the same resolved policy as `create_order_inner`, with the service charge on (a ticket is dine-in by definition, ruling 2). For a settled ticket the figures are the ORDER's, as booked, not a repricing under today's policy.
    pub fn new(
        discount_amount: i32,
        service_charge_amount: i32,
        service_charge_rate: f64,
        subtotal: i32,
        tax_amount: i32,
        tax_inclusive: bool,
        tax_rate: f64,
        total: i32,
    ) -> TicketBill {
        TicketBill {
            discount_amount,
            service_charge_amount,
            service_charge_rate,
            subtotal,
            tax_amount,
            tax_inclusive,
            tax_rate,
            total,
        }
    }
}
