/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// WalletProvider : What a wallet needs before it will offer a button, and whether it has it.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct WalletProvider {
    /// Everything present. False means the button is not offered at all.
    #[serde(rename = "configured")]
    pub configured: bool,
    #[serde(
        rename = "detail",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub detail: Option<Option<String>>,
    /// The settings still missing, by name. Empty when `configured`.
    #[serde(rename = "missing")]
    pub missing: Vec<String>,
    /// Google only: what Google itself said when asked. `None` for Apple, which signs locally and has nobody to ask.
    #[serde(
        rename = "reachable",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub reachable: Option<Option<bool>>,
}

impl WalletProvider {
    /// What a wallet needs before it will offer a button, and whether it has it.
    pub fn new(configured: bool, missing: Vec<String>) -> WalletProvider {
        WalletProvider {
            configured,
            detail: None,
            missing,
            reachable: None,
        }
    }
}
