/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// AttendanceStatus : The five mutually-exclusive states an attendance row can be in.  Stored as `text` with a CHECK constraint rather than a Postgres enum: statuses here are a closed set we control, and text avoids the `ALTER TYPE ... ADD VALUE` transaction dance every time the set grows.
/// The five mutually-exclusive states an attendance row can be in.  Stored as `text` with a CHECK constraint rather than a Postgres enum: statuses here are a closed set we control, and text avoids the `ALTER TYPE ... ADD VALUE` transaction dance every time the set grows.
#[derive(Clone, Copy, Debug, Eq, PartialEq, Ord, PartialOrd, Hash, Serialize, Deserialize)]
pub enum AttendanceStatus {
    #[serde(rename = "present")]
    Present,
    #[serde(rename = "late")]
    Late,
    #[serde(rename = "absent")]
    Absent,
    #[serde(rename = "half_day")]
    HalfDay,
    #[serde(rename = "on_leave")]
    OnLeave,

}

impl std::fmt::Display for AttendanceStatus {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        match self {
            Self::Present => write!(f, "present"),
            Self::Late => write!(f, "late"),
            Self::Absent => write!(f, "absent"),
            Self::HalfDay => write!(f, "half_day"),
            Self::OnLeave => write!(f, "on_leave"),
        }
    }
}

impl Default for AttendanceStatus {
    fn default() -> AttendanceStatus {
        Self::Present
    }
}

