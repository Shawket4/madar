/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// TillStatus : OpenAPI-only vocabulary for `Till.status` (the `till_status` DB enum). The struct fields stay `String`, so the wire strings are exactly the DB values.
/// OpenAPI-only vocabulary for `Till.status` (the `till_status` DB enum). The struct fields stay `String`, so the wire strings are exactly the DB values.
#[derive(Clone, Copy, Debug, Eq, PartialEq, Ord, PartialOrd, Hash, Serialize, Deserialize)]
pub enum TillStatus {
    #[serde(rename = "open")]
    Open,
    #[serde(rename = "closed")]
    Closed,
    #[serde(rename = "force_closed")]
    ForceClosed,
}

impl std::fmt::Display for TillStatus {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        match self {
            Self::Open => write!(f, "open"),
            Self::Closed => write!(f, "closed"),
            Self::ForceClosed => write!(f, "force_closed"),
        }
    }
}

impl Default for TillStatus {
    fn default() -> TillStatus {
        Self::Open
    }
}
