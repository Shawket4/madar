/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct PreviewCost {
    /// At least one deducted line has no cost: `total` is partial.
    #[serde(rename = "cost_missing")]
    pub cost_missing: bool,
    /// `(price − cost) / price` (fraction, like `/costing`); null when the cost is partial or the price is 0.
    #[serde(
        rename = "margin_pct",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub margin_pct: Option<Option<f64>>,
    /// Piastres over the deducted lines with a known cost.
    #[serde(rename = "total")]
    pub total: i64,
}

impl PreviewCost {
    pub fn new(cost_missing: bool, total: i64) -> PreviewCost {
        PreviewCost {
            cost_missing,
            margin_pct: None,
            total,
        }
    }
}
