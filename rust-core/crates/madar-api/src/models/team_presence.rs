/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// TeamPresence : The whole team's state right now, plus the day's labour against plan.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct TeamPresence {
    #[serde(rename = "absent")]
    pub absent: i64,
    /// The branch's business date, in ITS timezone — not the manager's device.
    #[serde(rename = "business_date")]
    pub business_date: chrono::NaiveDate,
    #[serde(rename = "late")]
    pub late: i64,
    #[serde(rename = "on_leave")]
    pub on_leave: i64,
    /// Minutes rostered for today across the team.
    #[serde(rename = "planned_minutes")]
    pub planned_minutes: i64,
    #[serde(rename = "present")]
    pub present: i64,
    #[serde(rename = "rows")]
    pub rows: Vec<models::PresenceRow>,
    /// Minutes actually worked so far today across the team.
    #[serde(rename = "worked_minutes")]
    pub worked_minutes: i64,
}

impl TeamPresence {
    /// The whole team's state right now, plus the day's labour against plan.
    pub fn new(
        absent: i64,
        business_date: chrono::NaiveDate,
        late: i64,
        on_leave: i64,
        planned_minutes: i64,
        present: i64,
        rows: Vec<models::PresenceRow>,
        worked_minutes: i64,
    ) -> TeamPresence {
        TeamPresence {
            absent,
            business_date,
            late,
            on_leave,
            planned_minutes,
            present,
            rows,
            worked_minutes,
        }
    }
}
