/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// PublicTableBill : What the table has ordered so far.  The page draws this above the menu, so a customer who scans halfway through a meal sees what is already on their bill rather than an empty basket that looks like a fresh start. It is also how the second, third and fourth person at the table see each other's rounds.  Public and unauthenticated, like everything else here. Whoever can read the code stuck to the table can see what that table ordered — which is the people sitting at it.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct PublicTableBill {
    /// When the party's bill was opened. The page counts up from this; a duration computed here would be wrong by the time it arrived.
    #[serde(rename = "opened_at")]
    pub opened_at: chrono::DateTime<chrono::FixedOffset>,
    /// The kitchen has finished everything fired so far.
    #[serde(rename = "ready")]
    pub ready: bool,
    /// Every round fired, oldest first, with what went to the kitchen in each.
    #[serde(rename = "rounds")]
    pub rounds: Vec<models::PublicTableRound>,
    /// Lines as charged, before discount — the bill's first line, not the bill.
    #[serde(rename = "subtotal")]
    pub subtotal: i32,
    #[serde(rename = "ticket_id")]
    pub ticket_id: uuid::Uuid,
    /// What the table will be asked to pay, as the SERVER prices it: the discount, the service charge and the tax are all in here, and none of them is something this page should be recomputing.
    #[serde(rename = "total")]
    pub total: i32,
}

impl PublicTableBill {
    /// What the table has ordered so far.  The page draws this above the menu, so a customer who scans halfway through a meal sees what is already on their bill rather than an empty basket that looks like a fresh start. It is also how the second, third and fourth person at the table see each other's rounds.  Public and unauthenticated, like everything else here. Whoever can read the code stuck to the table can see what that table ordered — which is the people sitting at it.
    pub fn new(
        opened_at: chrono::DateTime<chrono::FixedOffset>,
        ready: bool,
        rounds: Vec<models::PublicTableRound>,
        subtotal: i32,
        ticket_id: uuid::Uuid,
        total: i32,
    ) -> PublicTableBill {
        PublicTableBill {
            opened_at,
            ready,
            rounds,
            subtotal,
            ticket_id,
            total,
        }
    }
}
