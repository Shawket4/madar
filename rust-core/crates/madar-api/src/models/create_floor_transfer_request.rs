/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// CreateFloorTransferRequest : Operational table-state edit from the POS: the layout (geometry/shape) is dashboard-authored, but STATE — status walks (bussing a dirty table) and which zone the physical table currently sits in — belongs to the floor staff. Both fields optional; `clear_section` moves the table out of every section (`section_id` wins when both are sent).
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct CreateFloorTransferRequest {
    #[serde(rename = "branch_id")]
    pub branch_id: uuid::Uuid,
    /// Client-minted id (offline-first identity; retries dedup on it).
    #[serde(rename = "id")]
    pub id: uuid::Uuid,
    #[serde(
        rename = "note",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub note: Option<Option<String>>,
    #[serde(rename = "occupant_id")]
    pub occupant_id: uuid::Uuid,
    /// `held_order` | `open_ticket`.
    #[serde(rename = "occupant_kind")]
    pub occupant_kind: String,
    /// The wish: any table in this section…
    #[serde(
        rename = "target_section_id",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub target_section_id: Option<Option<uuid::Uuid>>,
    /// …or exactly this table. At least one of the two is required.
    #[serde(
        rename = "target_table_id",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub target_table_id: Option<Option<uuid::Uuid>>,
}

impl CreateFloorTransferRequest {
    /// Operational table-state edit from the POS: the layout (geometry/shape) is dashboard-authored, but STATE — status walks (bussing a dirty table) and which zone the physical table currently sits in — belongs to the floor staff. Both fields optional; `clear_section` moves the table out of every section (`section_id` wins when both are sent).
    pub fn new(
        branch_id: uuid::Uuid,
        id: uuid::Uuid,
        occupant_id: uuid::Uuid,
        occupant_kind: String,
    ) -> CreateFloorTransferRequest {
        CreateFloorTransferRequest {
            branch_id,
            id,
            note: None,
            occupant_id,
            occupant_kind,
            target_section_id: None,
            target_table_id: None,
        }
    }
}
