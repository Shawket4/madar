/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// DealApplicationInput : An applied deal on an order (the till's teller applied it). Live, the server re-prices it exactly over these units (`409 DEAL_NOT_ELIGIBLE` when they don't satisfy the rule; `orders.deals.apply` required). On replay the till's `discount` is kept and the server's verdict stored beside it.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct DealApplicationInput {
    #[serde(rename = "deal_rule_id")]
    pub deal_rule_id: uuid::Uuid,
    /// Replay only: what the till took off, piastres.
    #[serde(
        rename = "discount",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub discount: Option<Option<i32>>,
    #[serde(rename = "lines")]
    pub lines: Vec<models::DealLineInput>,
    #[serde(rename = "times")]
    pub times: i32,
}

impl DealApplicationInput {
    /// An applied deal on an order (the till's teller applied it). Live, the server re-prices it exactly over these units (`409 DEAL_NOT_ELIGIBLE` when they don't satisfy the rule; `orders.deals.apply` required). On replay the till's `discount` is kept and the server's verdict stored beside it.
    pub fn new(
        deal_rule_id: uuid::Uuid,
        lines: Vec<models::DealLineInput>,
        times: i32,
    ) -> DealApplicationInput {
        DealApplicationInput {
            deal_rule_id,
            discount: None,
            lines,
            times,
        }
    }
}
