/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// WalletStep : One request to Google and what it answered, kept verbatim.  Provisioning is four requests deep and every one of them can fail in a way the customer never sees: a refused class, an image Google will not fetch, a field it silently drops. A failed REFRESH is deliberately only a warning — the customer keeps the card they have — which means the reason lands in a log nobody is reading at the moment it matters.  So the same code path can be asked to keep a transcript. `save_url` throws it away; the super-admin diagnostic returns it. One path, so what the diagnostic reports is what actually happens, rather than a second implementation that agrees with the first until it doesn't.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct WalletStep {
    /// Google's answer, as it came. Truncated only if it is enormous.
    #[serde(rename = "body")]
    pub body: String,
    /// HTTP status, or 0 when the request never reached Google.
    #[serde(rename = "status")]
    pub status: u32,
    /// What was attempted, in words: \"insert the class\", \"update the object\".
    #[serde(rename = "step")]
    pub step: String,
}

impl WalletStep {
    /// One request to Google and what it answered, kept verbatim.  Provisioning is four requests deep and every one of them can fail in a way the customer never sees: a refused class, an image Google will not fetch, a field it silently drops. A failed REFRESH is deliberately only a warning — the customer keeps the card they have — which means the reason lands in a log nobody is reading at the moment it matters.  So the same code path can be asked to keep a transcript. `save_url` throws it away; the super-admin diagnostic returns it. One path, so what the diagnostic reports is what actually happens, rather than a second implementation that agrees with the first until it doesn't.
    pub fn new(body: String, status: u32, step: String) -> WalletStep {
        WalletStep { body, status, step }
    }
}
