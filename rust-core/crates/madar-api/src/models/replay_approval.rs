/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// ReplayApproval : A manager's approval carried by a queued op (PERMISSIONS_ARCHITECTURE §4.2), or — the same shape — a live request's one-time manager-PIN unlock (owner, 2026-09-17). `Serialize` + `ToSchema` are for the live half: the offline queue only ever deserializes one from the wire.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct ReplayApproval {
    #[serde(
        rename = "amount_minor",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub amount_minor: Option<Option<i64>>,
    #[serde(rename = "approver_id")]
    pub approver_id: uuid::Uuid,
    /// Capability key, e.g. `orders.void`.
    #[serde(rename = "capability")]
    pub capability: String,
    #[serde(rename = "id")]
    pub id: uuid::Uuid,
    /// Basis points, for an act capped by `max_percent` (a discount). Additive.
    #[serde(
        rename = "percent_bps",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub percent_bps: Option<Option<i64>>,
    /// The value an approval covered (`max_value` limits, e.g. a waste).
    #[serde(
        rename = "value_minor",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub value_minor: Option<Option<i64>>,
}

impl ReplayApproval {
    /// A manager's approval carried by a queued op (PERMISSIONS_ARCHITECTURE §4.2), or — the same shape — a live request's one-time manager-PIN unlock (owner, 2026-09-17). `Serialize` + `ToSchema` are for the live half: the offline queue only ever deserializes one from the wire.
    pub fn new(approver_id: uuid::Uuid, capability: String, id: uuid::Uuid) -> ReplayApproval {
        ReplayApproval {
            amount_minor: None,
            approver_id,
            capability,
            id,
            percent_bps: None,
            value_minor: None,
        }
    }
}
