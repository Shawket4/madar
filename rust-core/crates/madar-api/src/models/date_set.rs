/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// DateSet : A person's date that holds its own set (a date change), not the pattern.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct DateSet {
    #[serde(rename = "date")]
    pub date: chrono::NaiveDate,
    /// The date is a day off by date change (it holds no shift).
    #[serde(rename = "day_off")]
    pub day_off: bool,
    #[serde(rename = "employee_id")]
    pub employee_id: uuid::Uuid,
}

impl DateSet {
    /// A person's date that holds its own set (a date change), not the pattern.
    pub fn new(date: chrono::NaiveDate, day_off: bool, employee_id: uuid::Uuid) -> DateSet {
        DateSet {
            date,
            day_off,
            employee_id,
        }
    }
}
