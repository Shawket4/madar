/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// CardPreferences : What a customer can change about their own card, without an account.  The token in the URL is the credential — the same one their pass carries and the till scans. That is deliberate: a person who has just been messaged must be able to stop the messages by tapping the link in the message, not by remembering a password they never made.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct CardPreferences {
    /// The language they are reading this page in.  Sent by the page itself rather than chosen in a form. We stored whatever their phone said at signup, and a phone that has since changed language is a customer still being written to in the wrong one. Opening their own card is the moment we can tell.
    #[serde(rename = "locale", default, with = "::serde_with::rust::double_option", skip_serializing_if = "Option::is_none")]
    pub locale: Option<Option<String>>,
    /// Stop sending marketing. Covers the birthday greeting as well as the win-back: someone asking us to stop is asking the SHOP to stop, not to be excluded from one campaign.
    #[serde(rename = "marketing_opt_out", default, with = "::serde_with::rust::double_option", skip_serializing_if = "Option::is_none")]
    pub marketing_opt_out: Option<Option<bool>>,
}

impl CardPreferences {
    /// What a customer can change about their own card, without an account.  The token in the URL is the credential — the same one their pass carries and the till scans. That is deliberate: a person who has just been messaged must be able to stop the messages by tapping the link in the message, not by remembering a password they never made.
    pub fn new() -> CardPreferences {
        CardPreferences {
            locale: None,
            marketing_opt_out: None,
        }
    }
}

