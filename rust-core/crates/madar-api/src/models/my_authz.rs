/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// MyAuthz : What the signed-in person may do. The dashboard and POS gate on this.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct MyAuthz {
    /// Capabilities not held that show \"ask a manager\" instead of nothing.
    #[serde(rename = "ask_manager")]
    pub ask_manager: Vec<String>,
    #[serde(
        rename = "branch_id",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub branch_id: Option<Option<uuid::Uuid>>,
    /// Capability keys held.
    #[serde(rename = "capabilities")]
    pub capabilities: Vec<String>,
    #[serde(rename = "epoch")]
    pub epoch: i64,
    /// The capabilities held at EVERY branch of the business — what an org-wide act (a department, a shift block, a public holiday, the rules) needs. `/authz/me` only; absent elsewhere. (E2E B-SETUP-3)
    #[serde(
        rename = "everywhere",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub everywhere: Option<Option<Vec<String>>>,
    /// Limits on held capabilities, by key; absent = unlimited.
    #[serde(rename = "limits")]
    pub limits: std::collections::HashMap<String, models::LimitsView>,
    #[serde(rename = "owner")]
    pub owner: bool,
    #[serde(rename = "platform")]
    pub platform: bool,
    /// Role kinds held here (org_admin, branch_manager, teller, waiter, kitchen).
    #[serde(rename = "role_kinds")]
    pub role_kinds: Vec<String>,
    #[serde(rename = "spec_version")]
    pub spec_version: u32,
    #[serde(rename = "user_id")]
    pub user_id: uuid::Uuid,
}

impl MyAuthz {
    /// What the signed-in person may do. The dashboard and POS gate on this.
    pub fn new(
        ask_manager: Vec<String>,
        capabilities: Vec<String>,
        epoch: i64,
        limits: std::collections::HashMap<String, models::LimitsView>,
        owner: bool,
        platform: bool,
        role_kinds: Vec<String>,
        spec_version: u32,
        user_id: uuid::Uuid,
    ) -> MyAuthz {
        MyAuthz {
            ask_manager,
            branch_id: None,
            capabilities,
            epoch,
            everywhere: None,
            limits,
            owner,
            platform,
            role_kinds,
            spec_version,
            user_id,
        }
    }
}
