/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct MenuEngineeringRow {
    #[serde(rename = "category_id", default, with = "::serde_with::rust::double_option", skip_serializing_if = "Option::is_none")]
    pub category_id: Option<Option<uuid::Uuid>>,
    #[serde(rename = "category_name", default, with = "::serde_with::rust::double_option", skip_serializing_if = "Option::is_none")]
    pub category_name: Option<Option<String>>,
    /// star | workhorse | challenge | dog (Foodics names).
    #[serde(rename = "class")]
    pub class: String,
    /// Lines in the window whose sale-time cost could not be resolved. Always reports snapshot data quality, regardless of `cost_basis` — under `current`, an included row can still carry snapshot gaps.
    #[serde(rename = "cost_missing_lines")]
    pub cost_missing_lines: i64,
    #[serde(rename = "item_name")]
    pub item_name: String,
    /// Average profit per unit, piastres (`(sales - cost) / qty`).
    #[serde(rename = "item_profit")]
    pub item_profit: i64,
    #[serde(rename = "menu_item_id")]
    pub menu_item_id: uuid::Uuid,
    /// \"high\" | \"low\" — Kasavana-Smith 70% rule (0.70 / n).
    #[serde(rename = "popularity_category")]
    pub popularity_category: String,
    /// Share of units among the rows in this report (cost-tracked only).
    #[serde(rename = "popularity_pct")]
    pub popularity_pct: f64,
    /// \"high\" | \"low\" — vs weighted-average per-unit profit.
    #[serde(rename = "profit_category")]
    pub profit_category: String,
    /// Units sold (standalone lines only — bundle lines are excluded so the per-unit economics stay clean; bundle performance has its own report).
    #[serde(rename = "quantity_sold")]
    pub quantity_sold: i64,
    /// Revenue from those lines, piastres.
    #[serde(rename = "sales")]
    pub sales: i64,
    /// `\"one_size\"` for items without sizes.
    #[serde(rename = "size_label")]
    pub size_label: String,
    /// Recipe-scope COGS in piastres (additive addons excluded — they have their own revenue and their own report). Snapshot basis: `SUM(unit_cost × quantity)`; current basis: today's recipe rollup × quantity. Rows where this is unresolvable are excluded from the report, so it is always present.
    #[serde(rename = "total_cost")]
    pub total_cost: i64,
    /// `sales - total_cost`, piastres.
    #[serde(rename = "total_profit")]
    pub total_profit: i64,
}

impl MenuEngineeringRow {
    pub fn new(class: String, cost_missing_lines: i64, item_name: String, item_profit: i64, menu_item_id: uuid::Uuid, popularity_category: String, popularity_pct: f64, profit_category: String, quantity_sold: i64, sales: i64, size_label: String, total_cost: i64, total_profit: i64) -> MenuEngineeringRow {
        MenuEngineeringRow {
            category_id: None,
            category_name: None,
            class,
            cost_missing_lines,
            item_name,
            item_profit,
            menu_item_id,
            popularity_category,
            popularity_pct,
            profit_category,
            quantity_sold,
            sales,
            size_label,
            total_cost,
            total_profit,
        }
    }
}

