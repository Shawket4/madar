/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct BulkReviewResult {
    /// An id this call could not resolve, and why. Never silently dropped.
    #[serde(rename = "pending")]
    pub pending: Vec<models::BulkReviewPending>,
    /// Ids that are now reviewed (already reviewed counts as resolved too — resubmitting the same batch never fails or double-records).
    #[serde(rename = "resolved")]
    pub resolved: Vec<i64>,
}

impl BulkReviewResult {
    pub fn new(pending: Vec<models::BulkReviewPending>, resolved: Vec<i64>) -> BulkReviewResult {
        BulkReviewResult { pending, resolved }
    }
}
