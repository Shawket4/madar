/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// SyncRecipeLine : One recipe line of a modifier option: which ingredient the option deducts (or swaps in, when `quantity = 0`). Base-unit, yield-normalized values.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct SyncRecipeLine {
    #[serde(rename = "ingredient_id")]
    pub ingredient_id: uuid::Uuid,
    /// Base-unit quantity, serialized as a string for numeric fidelity.
    #[serde(rename = "quantity")]
    pub quantity: String,
    #[serde(rename = "unit")]
    pub unit: String,
}

impl SyncRecipeLine {
    /// One recipe line of a modifier option: which ingredient the option deducts (or swaps in, when `quantity = 0`). Base-unit, yield-normalized values.
    pub fn new(ingredient_id: uuid::Uuid, quantity: String, unit: String) -> SyncRecipeLine {
        SyncRecipeLine {
            ingredient_id,
            quantity,
            unit,
        }
    }
}

