/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// UnsettledPeriod : An older month still to settle (hunt H2-P1).
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct UnsettledPeriod {
    #[serde(rename = "ends_on")]
    pub ends_on: chrono::NaiveDate,
    /// The month's net pay: live for a draft, the frozen payslips once approved.
    #[serde(rename = "net_total_piastres")]
    pub net_total_piastres: i64,
    /// Payslips marked paid (a 'none' mark counts); 0 for a draft.
    #[serde(rename = "paid_count")]
    pub paid_count: i64,
    /// People on the month's payroll.
    #[serde(rename = "people")]
    pub people: i64,
    #[serde(rename = "period_id")]
    pub period_id: uuid::Uuid,
    #[serde(rename = "starts_on")]
    pub starts_on: chrono::NaiveDate,
    /// `draft` (never approved) · `generated` (approved, someone unpaid)
    #[serde(rename = "status")]
    pub status: String,
}

impl UnsettledPeriod {
    /// An older month still to settle (hunt H2-P1).
    pub fn new(
        ends_on: chrono::NaiveDate,
        net_total_piastres: i64,
        paid_count: i64,
        people: i64,
        period_id: uuid::Uuid,
        starts_on: chrono::NaiveDate,
        status: String,
    ) -> UnsettledPeriod {
        UnsettledPeriod {
            ends_on,
            net_total_piastres,
            paid_count,
            people,
            period_id,
            starts_on,
            status,
        }
    }
}
