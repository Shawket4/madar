/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// PublicSocialLink : One place the shop can be found, as a page prints it.  The same three things the wallet passes render (`wallet::apple`, `wallet::google`), so the card in the phone and the card on the page list the same links in the same order.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct PublicSocialLink {
    /// One of `orgs::social::PLATFORMS` — what the page picks its glyph by.
    #[serde(rename = "key")]
    pub key: String,
    /// What a human calls it. The page falls back to this where it has no glyph for `key`, so a platform added on the server still renders.
    #[serde(rename = "label")]
    pub label: String,
    /// `https://…` and nothing else — checked on write and again on read, see `orgs::social::links_of`.
    #[serde(rename = "url")]
    pub url: String,
}

impl PublicSocialLink {
    /// One place the shop can be found, as a page prints it.  The same three things the wallet passes render (`wallet::apple`, `wallet::google`), so the card in the phone and the card on the page list the same links in the same order.
    pub fn new(key: String, label: String, url: String) -> PublicSocialLink {
        PublicSocialLink { key, label, url }
    }
}
