/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// DeliveryModifierGroup : A per-item modifier group from the unified model (`menu_item_modifier_groups` → `modifier_groups`/`modifier_options`), constraints resolved (attachment overrides beat group defaults) and options already filtered to the attachment's `included_option_ids`. Only addon-sourced options appear here — the item's priced optionals stay in `optionals`. Empty until the org's catalog is backfilled onto the unified tables; the customizer falls back to the flat `addons` catalog + `allowed_addon_ids` in that case.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct DeliveryModifierGroup {
    /// The group's legacy addon type (`milk_type` / `coffee_type` / `extra` / custom) — the swap-family hint the customizer keys its delta-price estimate on. `None` for groups with no legacy lineage.
    #[serde(rename = "addon_type", default, with = "::serde_with::rust::double_option", skip_serializing_if = "Option::is_none")]
    pub addon_type: Option<Option<String>>,
    #[serde(rename = "group_id")]
    pub group_id: uuid::Uuid,
    #[serde(rename = "is_required")]
    pub is_required: bool,
    #[serde(rename = "max_selections", default, with = "::serde_with::rust::double_option", skip_serializing_if = "Option::is_none")]
    pub max_selections: Option<Option<i32>>,
    #[serde(rename = "min_selections")]
    pub min_selections: i32,
    #[serde(rename = "name")]
    pub name: String,
    #[serde(rename = "name_translations")]
    pub name_translations: serde_json::Value,
    #[serde(rename = "options")]
    pub options: Vec<models::DeliveryModifierOption>,
    /// \"single\" | \"multi\".
    #[serde(rename = "selection_type")]
    pub selection_type: String,
}

impl DeliveryModifierGroup {
    /// A per-item modifier group from the unified model (`menu_item_modifier_groups` → `modifier_groups`/`modifier_options`), constraints resolved (attachment overrides beat group defaults) and options already filtered to the attachment's `included_option_ids`. Only addon-sourced options appear here — the item's priced optionals stay in `optionals`. Empty until the org's catalog is backfilled onto the unified tables; the customizer falls back to the flat `addons` catalog + `allowed_addon_ids` in that case.
    pub fn new(group_id: uuid::Uuid, is_required: bool, min_selections: i32, name: String, name_translations: serde_json::Value, options: Vec<models::DeliveryModifierOption>, selection_type: String) -> DeliveryModifierGroup {
        DeliveryModifierGroup {
            addon_type: None,
            group_id,
            is_required,
            max_selections: None,
            min_selections,
            name,
            name_translations,
            options,
            selection_type,
        }
    }
}

