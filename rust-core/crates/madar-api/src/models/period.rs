/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// Period : The reporting window.  Prefer a [`PeriodPreset`]: it is resolved server-side against the merchant's timezone at query time, which means a dashboard widget saying \"last 30 days\" stays correct forever, and a language model never has to do calendar arithmetic — historically the single largest source of wrong answers.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct Period {
    /// Explicit inclusive lower bound.
    #[serde(
        rename = "from",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub from: Option<Option<String>>,
    /// A named relative window. Takes precedence over `from`/`to`.
    #[serde(
        rename = "preset",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub preset: Option<Option<models::PeriodPreset>>,
    /// Explicit inclusive upper bound.
    #[serde(
        rename = "to",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub to: Option<Option<String>>,
}

impl Period {
    /// The reporting window.  Prefer a [`PeriodPreset`]: it is resolved server-side against the merchant's timezone at query time, which means a dashboard widget saying \"last 30 days\" stays correct forever, and a language model never has to do calendar arithmetic — historically the single largest source of wrong answers.
    pub fn new() -> Period {
        Period {
            from: None,
            preset: None,
            to: None,
        }
    }
}
