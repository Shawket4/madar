/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// DeliveryAddonOption : One option in the org-wide addon catalog. The catalog is global (the POS model): every item can use any addon; swap-vs-additive is decided server-side from the addon `type` + the item recipe at order time. `price` is the channel-effective surcharge in piastres (branch_channel → branch → catalog default). Channel-unavailable options are excluded from the catalog entirely.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct DeliveryAddonOption {
    #[serde(rename = "addon_item_id")]
    pub addon_item_id: uuid::Uuid,
    #[serde(rename = "is_available")]
    pub is_available: bool,
    #[serde(rename = "name")]
    pub name: String,
    #[serde(rename = "name_translations")]
    pub name_translations: serde_json::Value,
    /// Channel-effective surcharge (piastres). Always present (resolved here).
    #[serde(rename = "price")]
    pub price: i32,
    /// `milk_type` | `coffee_type` | `extra` — the option's category.
    #[serde(rename = "type")]
    pub r#type: String,
}

impl DeliveryAddonOption {
    /// One option in the org-wide addon catalog. The catalog is global (the POS model): every item can use any addon; swap-vs-additive is decided server-side from the addon `type` + the item recipe at order time. `price` is the channel-effective surcharge in piastres (branch_channel → branch → catalog default). Channel-unavailable options are excluded from the catalog entirely.
    pub fn new(addon_item_id: uuid::Uuid, is_available: bool, name: String, name_translations: serde_json::Value, price: i32, r#type: String) -> DeliveryAddonOption {
        DeliveryAddonOption {
            addon_item_id,
            is_available,
            name,
            name_translations,
            price,
            r#type,
        }
    }
}

