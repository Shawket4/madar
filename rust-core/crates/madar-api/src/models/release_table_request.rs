/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct ReleaseTableRequest {
    #[serde(rename = "branch_id")]
    pub branch_id: uuid::Uuid,
    /// The party ATE here and has paid: the table needs bussing before anyone else sits, so it lands `dirty` rather than `free`. The same fork the till makes locally when a parked order checks out versus is discarded -- a discard means nobody ever sat, and the table goes straight back to the room. Without this the dashboard would show a table with dirty plates on it as ready for the next party.
    #[serde(rename = "bus", skip_serializing_if = "Option::is_none")]
    pub bus: Option<bool>,
}

impl ReleaseTableRequest {
    pub fn new(branch_id: uuid::Uuid) -> ReleaseTableRequest {
        ReleaseTableRequest {
            branch_id,
            bus: None,
        }
    }
}
