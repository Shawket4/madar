/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// CardBrand : How a tenant's card should look.  Every field is optional and the site falls back to Madar's own palette, so a tenant who has set nothing still gets a finished card rather than an unstyled one. `org_name` is NOT optional: whose card this is must always be on it, however little else has been configured.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct CardBrand {
    /// `#RRGGBB`, validated on write.
    #[serde(
        rename = "background_color",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub background_color: Option<Option<String>>,
    /// The wide photograph across the card — Apple's strip, Google's hero image, and the band at the top of the web card. Absent is a finished card, not a broken one.
    #[serde(
        rename = "card_image_url",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub card_image_url: Option<Option<String>>,
    #[serde(
        rename = "foreground_color",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub foreground_color: Option<Option<String>>,
    #[serde(
        rename = "label_color",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub label_color: Option<Option<String>>,
    /// True when the logo is a shape on transparency, so the card may repaint it in the foreground for contrast. False for a logo with its background baked in, which gets a plate to sit on instead — repainting that one would give a solid rectangle. See `orgs::branding::is_mark`.
    #[serde(rename = "logo_is_mark")]
    pub logo_is_mark: bool,
    #[serde(
        rename = "logo_url",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub logo_url: Option<Option<String>>,
    /// The organisation's name. Always present.
    #[serde(rename = "org_name")]
    pub org_name: String,
    /// What the programme calls itself (\"Rewards\", \"Bean Club\").
    #[serde(rename = "program_name")]
    pub program_name: String,
    #[serde(
        rename = "program_name_ar",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub program_name_ar: Option<Option<String>>,
    /// Where else to find the shop, in the order a card prints them. Empty is the common case, and the page draws nothing for it — no row, no placeholder.  NOT gated on the branding tier, like `OrgBrand::social_links` it is read from: a shop's Instagram is a fact about the shop in the way its name is, so a Madar-coloured card carries the links too.
    #[serde(rename = "social_links")]
    pub social_links: Vec<models::PublicSocialLink>,
}

impl CardBrand {
    /// How a tenant's card should look.  Every field is optional and the site falls back to Madar's own palette, so a tenant who has set nothing still gets a finished card rather than an unstyled one. `org_name` is NOT optional: whose card this is must always be on it, however little else has been configured.
    pub fn new(
        logo_is_mark: bool,
        org_name: String,
        program_name: String,
        social_links: Vec<models::PublicSocialLink>,
    ) -> CardBrand {
        CardBrand {
            background_color: None,
            card_image_url: None,
            foreground_color: None,
            label_color: None,
            logo_is_mark,
            logo_url: None,
            org_name,
            program_name,
            program_name_ar: None,
            social_links,
        }
    }
}
