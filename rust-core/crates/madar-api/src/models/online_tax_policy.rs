/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// OnlineTaxPolicy : What the storefront needs to render a bill honestly: the rate, and whether the menu prices already contain it. Sent with the branch list and with every quote so a page can show a tax line (exclusive) or an \"includes VAT\" note (inclusive) instead of quietly under-quoting. No service-charge fields: an online order never carries one, and a field that is always zero invites a page to render a line for it.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct OnlineTaxPolicy {
    /// `true` = menu prices already contain the tax; the total will not grow.
    #[serde(rename = "tax_inclusive")]
    pub tax_inclusive: bool,
    /// Fraction, NOT a percentage: `0.14` is 14%.
    #[serde(rename = "tax_rate")]
    pub tax_rate: f64,
}

impl OnlineTaxPolicy {
    /// What the storefront needs to render a bill honestly: the rate, and whether the menu prices already contain it. Sent with the branch list and with every quote so a page can show a tax line (exclusive) or an \"includes VAT\" note (inclusive) instead of quietly under-quoting. No service-charge fields: an online order never carries one, and a field that is always zero invites a page to render a line for it.
    pub fn new(tax_inclusive: bool, tax_rate: f64) -> OnlineTaxPolicy {
        OnlineTaxPolicy {
            tax_inclusive,
            tax_rate,
        }
    }
}
