/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// PublicCartQuoteRequest : `POST /public/branches/{id}/cart-quote` (online) and `POST /public/tables/{id}/cart-quote` (QR): the cart priced by the server exactly as the order will be, with the best deals applied automatically.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct PublicCartQuoteRequest {
    /// Online only: the delivery sub-channel whose prices apply (`in_mall` | `outside` | `umbrella` | `pickup`); default `pickup`.
    #[serde(
        rename = "channel",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub channel: Option<Option<String>>,
    #[serde(rename = "items")]
    pub items: Vec<models::OrderItemInput>,
}

impl PublicCartQuoteRequest {
    /// `POST /public/branches/{id}/cart-quote` (online) and `POST /public/tables/{id}/cart-quote` (QR): the cart priced by the server exactly as the order will be, with the best deals applied automatically.
    pub fn new(items: Vec<models::OrderItemInput>) -> PublicCartQuoteRequest {
        PublicCartQuoteRequest {
            channel: None,
            items,
        }
    }
}
