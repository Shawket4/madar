/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct CreateRefundRequest {
    /// Minor units, > 0. Together with every refund already on the order it may not exceed `orders.total_amount`.
    #[serde(rename = "amount")]
    pub amount: i32,
    /// Client-minted idempotency key. A retried request or a replayed offline queue carrying the same key gets the original refund back instead of handing the money out again.
    #[serde(
        rename = "client_ref",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub client_ref: Option<Option<uuid::Uuid>>,
    /// When the refund was issued. Omit for live requests — the server stamps `now()`. An offline till sends the real time; future values are rejected.
    #[serde(
        rename = "issued_at",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub issued_at: Option<Option<chrono::DateTime<chrono::FixedOffset>>>,
    #[serde(rename = "lines", skip_serializing_if = "Option::is_none")]
    pub lines: Option<Vec<models::RefundLineInput>>,
    /// How the money went back — a name from the org's payment-method vocabulary. One tender per refund; a split is two refunds.
    #[serde(rename = "method")]
    pub method: String,
    /// Free-text explanation. Required when `reason` is `other`.
    #[serde(
        rename = "note",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub note: Option<Option<String>>,
    /// The settled sale the money goes back against.
    #[serde(rename = "order_id")]
    pub order_id: uuid::Uuid,
    #[serde(rename = "reason")]
    pub reason: models::RefundReason,
    /// The shift whose drawer the money leaves. Omit for a live request and the actor's own open shift at the order's branch is used; a replayed offline refund must name the shift it was issued in, the way a queued sale does.
    #[serde(
        rename = "shift_id",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub shift_id: Option<Option<uuid::Uuid>>,
}

impl CreateRefundRequest {
    pub fn new(
        amount: i32,
        method: String,
        order_id: uuid::Uuid,
        reason: models::RefundReason,
    ) -> CreateRefundRequest {
        CreateRefundRequest {
            amount,
            client_ref: None,
            issued_at: None,
            lines: None,
            method,
            note: None,
            order_id,
            reason,
            shift_id: None,
        }
    }
}
