/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// ComboFeed : The `combo` object on a `kind=combo` row of `GET /menu-items?full=true`, `/catalog/sync` and the `/sync/pull` `menu_item` row. The price is in the row's usual price fields. Choices keep `category_id`: the till expands a category from its own menu.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct ComboFeed {
    #[serde(rename = "is_fixed")]
    pub is_fixed: bool,
    /// The org's channel toggles, resolved for the requested branch.
    #[serde(rename = "sell")]
    pub sell: Box<models::ChannelToggles>,
    #[serde(rename = "slots")]
    pub slots: Vec<models::ComboSlot>,
    /// Only the windows for this branch or for every branch.
    #[serde(rename = "windows")]
    pub windows: Vec<models::SaleWindow>,
}

impl ComboFeed {
    /// The `combo` object on a `kind=combo` row of `GET /menu-items?full=true`, `/catalog/sync` and the `/sync/pull` `menu_item` row. The price is in the row's usual price fields. Choices keep `category_id`: the till expands a category from its own menu.
    pub fn new(
        is_fixed: bool,
        sell: models::ChannelToggles,
        slots: Vec<models::ComboSlot>,
        windows: Vec<models::SaleWindow>,
    ) -> ComboFeed {
        ComboFeed {
            is_fixed,
            sell: Box::new(sell),
            slots,
            windows,
        }
    }
}
