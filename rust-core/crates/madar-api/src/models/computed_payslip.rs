/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// ComputedPayslip : One employee's pay for a period, computed but not yet written.  PREVIEW AND GENERATE SHARE THIS. The preview endpoint exists so a manager can see what payroll is about to do — a figure that would be worthless if it came from a second implementation that could drift from the real one. So the generator computes these first and then persists them, and the preview computes exactly the same values and persists nothing.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct ComputedPayslip {
    #[serde(rename = "absent_days")]
    pub absent_days: f64,
    /// What the advances WANT versus what the payslip can afford differ when net pay would go negative; this is the affordable figure, the one collected.
    #[serde(rename = "advance_installment_piastres")]
    pub advance_installment_piastres: i64,
    /// After the attendance proration — what the days actually worked earn.
    #[serde(rename = "base_piastres")]
    pub base_piastres: i64,
    #[serde(rename = "base_salary_piastres")]
    pub base_salary_piastres: i64,
    #[serde(rename = "bonuses_piastres")]
    pub bonuses_piastres: i64,
    /// Line-by-line, so a preview can name each deduction rather than showing a lump sum nobody can argue with.
    #[serde(rename = "breakdown", deserialize_with = "Option::deserialize")]
    pub breakdown: Option<serde_json::Value>,
    #[serde(rename = "deductions_piastres")]
    pub deductions_piastres: i64,
    #[serde(rename = "late_minutes")]
    pub late_minutes: i64,
    #[serde(rename = "leave_days")]
    pub leave_days: f64,
    #[serde(rename = "name")]
    pub name: String,
    #[serde(rename = "net_piastres")]
    pub net_piastres: i64,
    #[serde(rename = "overtime_minutes")]
    pub overtime_minutes: i64,
    #[serde(rename = "overtime_piastres")]
    pub overtime_piastres: i64,
    #[serde(rename = "user_id")]
    pub user_id: uuid::Uuid,
    #[serde(rename = "worked_days")]
    pub worked_days: f64,
}

impl ComputedPayslip {
    /// One employee's pay for a period, computed but not yet written.  PREVIEW AND GENERATE SHARE THIS. The preview endpoint exists so a manager can see what payroll is about to do — a figure that would be worthless if it came from a second implementation that could drift from the real one. So the generator computes these first and then persists them, and the preview computes exactly the same values and persists nothing.
    pub fn new(absent_days: f64, advance_installment_piastres: i64, base_piastres: i64, base_salary_piastres: i64, bonuses_piastres: i64, breakdown: Option<serde_json::Value>, deductions_piastres: i64, late_minutes: i64, leave_days: f64, name: String, net_piastres: i64, overtime_minutes: i64, overtime_piastres: i64, user_id: uuid::Uuid, worked_days: f64) -> ComputedPayslip {
        ComputedPayslip {
            absent_days,
            advance_installment_piastres,
            base_piastres,
            base_salary_piastres,
            bonuses_piastres,
            breakdown,
            deductions_piastres,
            late_minutes,
            leave_days,
            name,
            net_piastres,
            overtime_minutes,
            overtime_piastres,
            user_id,
            worked_days,
        }
    }
}

