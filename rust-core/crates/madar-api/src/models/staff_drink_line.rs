/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// StaffDrinkLine : `staff_drink` on an order line. Additive: a client that never heard of it omits it and the line is an ordinary paid line.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct StaffDrinkLine {
    /// What the TILL comped on this line (whole line, minor units). Read ONLY when a queued offline sale is replayed; live, the server prices the comp and this is ignored.
    #[serde(
        rename = "comp_minor",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub comp_minor: Option<Option<i32>>,
    /// Client-minted; the idempotency key AND the `staff_drinks` row's id. A row an older flow already recorded under this id is reused and the order attached to it — never a second drink off the allowance.
    #[serde(rename = "id")]
    pub id: uuid::Uuid,
    /// REQUIRED. Who the drink is for and why, in the teller's own words.
    #[serde(rename = "note")]
    pub note: String,
    /// Whether the till believed this drink went past the allowance. Replay only, and only to tell a convergence from a surprise.
    #[serde(
        rename = "overspent",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub overspent: Option<Option<bool>>,
}

impl StaffDrinkLine {
    /// `staff_drink` on an order line. Additive: a client that never heard of it omits it and the line is an ordinary paid line.
    pub fn new(id: uuid::Uuid, note: String) -> StaffDrinkLine {
        StaffDrinkLine {
            comp_minor: None,
            id,
            note,
            overspent: None,
        }
    }
}
