/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct TaxReport {
    #[serde(rename = "discount_amount")]
    pub discount_amount: i64,
    #[serde(
        rename = "from",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub from: Option<Option<chrono::DateTime<chrono::FixedOffset>>>,
    /// `total_amount`, net of refunds, across every branch.
    #[serde(rename = "net_revenue")]
    pub net_revenue: i64,
    /// `tax_collected - refunded_tax` — what is actually owed for the period.
    #[serde(rename = "net_tax_due")]
    pub net_tax_due: i64,
    #[serde(rename = "order_count")]
    pub order_count: i64,
    /// The org's current tax rate, as a decimal fraction. Informational only — individual orders carry the rate that was actually applied at sale time (`tax_rate_applied`), which may differ if the rate changed since.
    #[serde(rename = "org_tax_rate")]
    pub org_tax_rate: f64,
    #[serde(rename = "refunded_tax")]
    pub refunded_tax: i64,
    /// Net of refunded service charge.
    #[serde(rename = "service_charge_amount")]
    pub service_charge_amount: i64,
    /// Sum of `orders.subtotal` across every branch, before discount or tax.
    #[serde(rename = "subtotal")]
    pub subtotal: i64,
    /// Tax on every non-voided sale in the range, before refunds (a sale later refunded in full included).
    #[serde(rename = "tax_collected")]
    pub tax_collected: i64,
    #[serde(
        rename = "to",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub to: Option<Option<chrono::DateTime<chrono::FixedOffset>>>,
    #[serde(rename = "voided_orders")]
    pub voided_orders: i64,
}

impl TaxReport {
    pub fn new(
        discount_amount: i64,
        net_revenue: i64,
        net_tax_due: i64,
        order_count: i64,
        org_tax_rate: f64,
        refunded_tax: i64,
        service_charge_amount: i64,
        subtotal: i64,
        tax_collected: i64,
        voided_orders: i64,
    ) -> TaxReport {
        TaxReport {
            discount_amount,
            from: None,
            net_revenue,
            net_tax_due,
            order_count,
            org_tax_rate,
            refunded_tax,
            service_charge_amount,
            subtotal,
            tax_collected,
            to: None,
            voided_orders,
        }
    }
}
