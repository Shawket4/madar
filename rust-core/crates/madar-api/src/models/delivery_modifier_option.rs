/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// DeliveryModifierOption : One option inside a per-item modifier group. `option_id` is the STABLE id — it equals the legacy `addon_item_id`, so order intake accepts it unchanged in `addons[].addon_item_id` (menu-unification stable-id rule).
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct DeliveryModifierOption {
    #[serde(rename = "name")]
    pub name: String,
    #[serde(rename = "name_translations")]
    pub name_translations: serde_json::Value,
    #[serde(rename = "option_id")]
    pub option_id: uuid::Uuid,
    /// Channel-effective surcharge (piastres): branch_channel → branch → channel → catalog default. Unavailable options are excluded entirely.
    #[serde(rename = "price")]
    pub price: i32,
}

impl DeliveryModifierOption {
    /// One option inside a per-item modifier group. `option_id` is the STABLE id — it equals the legacy `addon_item_id`, so order intake accepts it unchanged in `addons[].addon_item_id` (menu-unification stable-id rule).
    pub fn new(name: String, name_translations: serde_json::Value, option_id: uuid::Uuid, price: i32) -> DeliveryModifierOption {
        DeliveryModifierOption {
            name,
            name_translations,
            option_id,
            price,
        }
    }
}

