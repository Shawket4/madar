/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// MyAttendanceToday : What the mobile app shows on its home screen.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct MyAttendanceToday {
    /// Why `can_check_in` is false, in words the app can show verbatim.
    #[serde(rename = "blocked_reason", default, with = "::serde_with::rust::double_option", skip_serializing_if = "Option::is_none")]
    pub blocked_reason: Option<Option<String>>,
    /// WHERE to clock in today. Resolved server-side — from the open record, the rostered shift's branch, or the employee's single branch assignment — so the app never has to ask. A branch picker would make the geofence answerable to a dropdown, which defeats the point of having one. `None` means we cannot tell, and the app should say so rather than guess.
    #[serde(rename = "branch_id", default, with = "::serde_with::rust::double_option", skip_serializing_if = "Option::is_none")]
    pub branch_id: Option<Option<uuid::Uuid>>,
    /// That branch's name, so the app's geofence chip can say WHERE it is about to clock in rather than merely that it can.
    #[serde(rename = "branch_name", default, with = "::serde_with::rust::double_option", skip_serializing_if = "Option::is_none")]
    pub branch_name: Option<Option<String>>,
    /// The business date in the relevant branch's timezone — not the device's.
    #[serde(rename = "business_date")]
    pub business_date: chrono::NaiveDate,
    #[serde(rename = "can_check_in")]
    pub can_check_in: bool,
    #[serde(rename = "can_check_out")]
    pub can_check_out: bool,
    /// Records already closed today.
    #[serde(rename = "closed_records")]
    pub closed_records: Vec<models::AttendanceRecord>,
    /// The still-open record, when the employee is currently clocked in.
    #[serde(rename = "open_record", default, with = "::serde_with::rust::double_option", skip_serializing_if = "Option::is_none")]
    pub open_record: Option<Option<Box<models::AttendanceRecord>>>,
    /// Shifts rostered for today. Empty = a rest day.
    #[serde(rename = "scheduled")]
    pub scheduled: Vec<models::ResolvedShift>,
}

impl MyAttendanceToday {
    /// What the mobile app shows on its home screen.
    pub fn new(business_date: chrono::NaiveDate, can_check_in: bool, can_check_out: bool, closed_records: Vec<models::AttendanceRecord>, scheduled: Vec<models::ResolvedShift>) -> MyAttendanceToday {
        MyAttendanceToday {
            blocked_reason: None,
            branch_id: None,
            branch_name: None,
            business_date,
            can_check_in,
            can_check_out,
            closed_records,
            open_record: None,
            scheduled,
        }
    }
}

