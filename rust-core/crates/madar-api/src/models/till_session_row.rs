/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// TillSessionRow : One till session, the way a manager reconciles a drawer: what was in it at open, what cash the shift put through it, what the teller declared at close, and what the system says should have been there.  Money is piastres. `null` means *not yet known* (an open till has no closing figures), never zero — a till still running and a till that counted zero are different facts.  The cash columns add up: `opening_cash + net_cash_payment + pay_ins − pay_outs − cash_drops + cash_adjustments` is the drawer's expected cash — `closing_cash_system` once the till is closed.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct TillSessionRow {
    /// The branch's short code, the prefix of its order references.
    #[serde(rename = "branch_code")]
    pub branch_code: String,
    #[serde(rename = "branch_id")]
    pub branch_id: uuid::Uuid,
    #[serde(rename = "branch_name")]
    pub branch_name: String,
    /// The branch-local calendar day the till was OPENED on — a till opened at 23:50 and closed at 02:10 belongs to the day it opened, which is the day the takings are reported under.
    #[serde(rename = "business_date")]
    pub business_date: chrono::NaiveDate,
    /// Corrections that name no movement of the three kinds above. Signed: positive = cash the drawer gained. The till report's `cash_adjustments`.
    #[serde(rename = "cash_adjustments")]
    pub cash_adjustments: i64,
    /// Declared − expected. Negative = short, positive = over.
    #[serde(
        rename = "cash_discrepancy",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub cash_discrepancy: Option<Option<i64>>,
    /// Cash moved to the safe, net of corrections to safe drops. Positive = the magnitude that left.
    #[serde(rename = "cash_drops")]
    pub cash_drops: i64,
    #[serde(
        rename = "closed_at",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub closed_at: Option<Option<chrono::DateTime<chrono::FixedOffset>>>,
    /// What was counted at close. `null` while the till is open, and for a force-close nobody counted.
    #[serde(
        rename = "closing_cash_declared",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub closing_cash_declared: Option<Option<i64>>,
    /// What the system expected at close. `null` while the till is open.
    #[serde(
        rename = "closing_cash_system",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub closing_cash_system: Option<Option<i64>>,
    /// Cash that came in over the counter: cash payments and cash tips on tendered sales, less cash handed back as refunds from this drawer.
    #[serde(rename = "net_cash_payment")]
    pub net_cash_payment: i64,
    /// Those sales' value, net of what refunds took back (`net_sales` in the POS metrics report, `revenue` in the teller report).
    #[serde(rename = "net_sales")]
    pub net_sales: i64,
    #[serde(rename = "opened_at")]
    pub opened_at: chrono::DateTime<chrono::FixedOffset>,
    #[serde(rename = "opening_cash")]
    pub opening_cash: i64,
    /// Sales rung on this till, voided and fully-refunded bills excluded — the same count the teller report uses.
    #[serde(rename = "orders_count")]
    pub orders_count: i64,
    /// Cash added to the drawer that is not a sale (a float top-up), net of corrections to pay-ins.
    #[serde(rename = "pay_ins")]
    pub pay_ins: i64,
    /// Cash spent out of the drawer, net of corrections to pay-outs. Positive = the magnitude that left.
    #[serde(rename = "pay_outs")]
    pub pay_outs: i64,
    /// `open` | `closed` | `force_closed`.
    #[serde(rename = "status")]
    pub status: String,
    #[serde(rename = "teller_id")]
    pub teller_id: uuid::Uuid,
    #[serde(rename = "teller_name")]
    pub teller_name: String,
    #[serde(rename = "till_id")]
    pub till_id: uuid::Uuid,
}

impl TillSessionRow {
    /// One till session, the way a manager reconciles a drawer: what was in it at open, what cash the shift put through it, what the teller declared at close, and what the system says should have been there.  Money is piastres. `null` means *not yet known* (an open till has no closing figures), never zero — a till still running and a till that counted zero are different facts.  The cash columns add up: `opening_cash + net_cash_payment + pay_ins − pay_outs − cash_drops + cash_adjustments` is the drawer's expected cash — `closing_cash_system` once the till is closed.
    pub fn new(
        branch_code: String,
        branch_id: uuid::Uuid,
        branch_name: String,
        business_date: chrono::NaiveDate,
        cash_adjustments: i64,
        cash_drops: i64,
        net_cash_payment: i64,
        net_sales: i64,
        opened_at: chrono::DateTime<chrono::FixedOffset>,
        opening_cash: i64,
        orders_count: i64,
        pay_ins: i64,
        pay_outs: i64,
        status: String,
        teller_id: uuid::Uuid,
        teller_name: String,
        till_id: uuid::Uuid,
    ) -> TillSessionRow {
        TillSessionRow {
            branch_code,
            branch_id,
            branch_name,
            business_date,
            cash_adjustments,
            cash_discrepancy: None,
            cash_drops,
            closed_at: None,
            closing_cash_declared: None,
            closing_cash_system: None,
            net_cash_payment,
            net_sales,
            opened_at,
            opening_cash,
            orders_count,
            pay_ins,
            pay_outs,
            status,
            teller_id,
            teller_name,
            till_id,
        }
    }
}
