/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// OptionRecipeLineInput : One recipe line as submitted to the option-recipe replace endpoint. `quantity` may be 0 (a swap marker). Server normalizes to the ingredient base unit.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct OptionRecipeLineInput {
    #[serde(rename = "ingredient_id")]
    pub ingredient_id: uuid::Uuid,
    #[serde(rename = "quantity")]
    pub quantity: f64,
    /// Size this amount is for (`Cup`, `Can`); `null`/absent = every size. At order time a line for the ordered size's exact label replaces the `null` line for the same ingredient. Legacy tills only ever see the `null` lines.
    #[serde(
        rename = "size_label",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub size_label: Option<Option<String>>,
    #[serde(rename = "unit")]
    pub unit: String,
}

impl OptionRecipeLineInput {
    /// One recipe line as submitted to the option-recipe replace endpoint. `quantity` may be 0 (a swap marker). Server normalizes to the ingredient base unit.
    pub fn new(ingredient_id: uuid::Uuid, quantity: f64, unit: String) -> OptionRecipeLineInput {
        OptionRecipeLineInput {
            ingredient_id,
            quantity,
            size_label: None,
            unit,
        }
    }
}
