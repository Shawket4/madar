/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct RewardItem {
    /// Menu price in piastres — what the reward is worth, for the admin's sake.
    #[serde(rename = "base_price")]
    pub base_price: i32,
    /// How much of that currency it costs. Per item, so one catalogue holds \"espresso, 5 visits\" beside \"cake, 10 visits\".
    #[serde(rename = "cost_amount")]
    pub cost_amount: i32,
    /// `\"points\"` or `\"visits\"` — what this reward is bought with.  Always the currency of the scope this was READ through, never whatever the row happened to be written with. A program has one mode ([`LoyaltySettings::mode`]) and a reward is priced in it; the stored column is a record of intent, not an independent fact, and letting the two disagree is what made a whole catalogue vanish from the till while the dashboard still listed it.
    #[serde(rename = "cost_currency")]
    pub cost_currency: String,
    #[serde(rename = "image_url", default, with = "::serde_with::rust::double_option", skip_serializing_if = "Option::is_none")]
    pub image_url: Option<Option<String>>,
    #[serde(rename = "menu_item_id")]
    pub menu_item_id: uuid::Uuid,
    /// Denormalised for display so the teller and the pass need no menu join.
    #[serde(rename = "name")]
    pub name: String,
    #[serde(rename = "sort_order")]
    pub sort_order: i32,
}

impl RewardItem {
    pub fn new(base_price: i32, cost_amount: i32, cost_currency: String, menu_item_id: uuid::Uuid, name: String, sort_order: i32) -> RewardItem {
        RewardItem {
            base_price,
            cost_amount,
            cost_currency,
            image_url: None,
            menu_item_id,
            name,
            sort_order,
        }
    }
}

