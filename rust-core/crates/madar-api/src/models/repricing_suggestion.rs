/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// RepricingSuggestion : One repricing suggestion: an underpriced SKU + the price that would restore the target margin.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct RepricingSuggestion {
    /// True when the item currently sells BELOW cost (negative margin).
    #[serde(rename = "below_cost")]
    pub below_cost: bool,
    /// Complete recipe cost, piastres.
    #[serde(rename = "cost")]
    pub cost: i64,
    /// Current selling price, piastres.
    #[serde(rename = "current_price")]
    pub current_price: i64,
    #[serde(rename = "item_name")]
    pub item_name: String,
    /// `(price − cost) / price`, current.
    #[serde(rename = "margin_pct")]
    pub margin_pct: f64,
    #[serde(rename = "menu_item_id")]
    pub menu_item_id: uuid::Uuid,
    /// `\"one_size\"` for items without sizes.
    #[serde(rename = "size_label")]
    pub size_label: String,
    /// Target-restoring price `ceil(cost / (1 − target))` to whole EGP, piastres.
    #[serde(rename = "suggested_price")]
    pub suggested_price: i64,
    /// The org/branch target margin this suggestion aims for.
    #[serde(rename = "target_pct")]
    pub target_pct: f64,
    /// `suggested_price − current_price`, piastres.
    #[serde(rename = "uplift")]
    pub uplift: i64,
}

impl RepricingSuggestion {
    /// One repricing suggestion: an underpriced SKU + the price that would restore the target margin.
    pub fn new(below_cost: bool, cost: i64, current_price: i64, item_name: String, margin_pct: f64, menu_item_id: uuid::Uuid, size_label: String, suggested_price: i64, target_pct: f64, uplift: i64) -> RepricingSuggestion {
        RepricingSuggestion {
            below_cost,
            cost,
            current_price,
            item_name,
            margin_pct,
            menu_item_id,
            size_label,
            suggested_price,
            target_pct,
            uplift,
        }
    }
}

