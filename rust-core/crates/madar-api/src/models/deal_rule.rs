/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// DealRule : A deal rule. `n_for_price`: any `qty` units of the pool for `price`. `buy_get`: buy `qty`, get `get_qty` at `get_percent`% off (100 = free), the rewarded units drawn from `reward_pool` (or the pool when empty). A deal covers the item's size price only; add-ons always pay.  Also the `deal_rule` feed row of `/sync/pull`, where `is_active` is resolved for the device's branch and `sell` carries the branch's channel toggles.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct DealRule {
    #[serde(rename = "branch_overrides")]
    pub branch_overrides: Vec<models::DealBranchOverride>,
    #[serde(rename = "created_at")]
    pub created_at: chrono::DateTime<chrono::FixedOffset>,
    #[serde(
        rename = "get_percent",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub get_percent: Option<Option<i32>>,
    #[serde(
        rename = "get_qty",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub get_qty: Option<Option<i32>>,
    #[serde(rename = "id")]
    pub id: uuid::Uuid,
    #[serde(rename = "is_active")]
    pub is_active: bool,
    /// `n_for_price` | `buy_get`.
    #[serde(rename = "kind")]
    pub kind: String,
    #[serde(
        rename = "max_per_order",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub max_per_order: Option<Option<i32>>,
    #[serde(rename = "name")]
    pub name: String,
    #[serde(rename = "name_translations")]
    pub name_translations: serde_json::Value,
    #[serde(rename = "pool")]
    pub pool: Vec<models::DealPoolEntry>,
    /// n_for_price: the price of `qty` units, piastres.
    #[serde(
        rename = "price",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub price: Option<Option<i32>>,
    #[serde(rename = "qty")]
    pub qty: i32,
    /// buy_get only; `[]` = the rewarded units come from `pool`.
    #[serde(rename = "reward_pool")]
    pub reward_pool: Vec<models::DealPoolEntry>,
    /// Feed rows only: the branch's channel toggles (§11.1). Absent elsewhere.
    #[serde(
        rename = "sell",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub sell: Option<Option<Box<models::ChannelToggles>>>,
    #[serde(rename = "sort")]
    pub sort: i32,
    #[serde(rename = "updated_at")]
    pub updated_at: chrono::DateTime<chrono::FixedOffset>,
    #[serde(rename = "windows")]
    pub windows: Vec<models::SaleWindow>,
}

impl DealRule {
    /// A deal rule. `n_for_price`: any `qty` units of the pool for `price`. `buy_get`: buy `qty`, get `get_qty` at `get_percent`% off (100 = free), the rewarded units drawn from `reward_pool` (or the pool when empty). A deal covers the item's size price only; add-ons always pay.  Also the `deal_rule` feed row of `/sync/pull`, where `is_active` is resolved for the device's branch and `sell` carries the branch's channel toggles.
    pub fn new(
        branch_overrides: Vec<models::DealBranchOverride>,
        created_at: chrono::DateTime<chrono::FixedOffset>,
        id: uuid::Uuid,
        is_active: bool,
        kind: String,
        name: String,
        name_translations: serde_json::Value,
        pool: Vec<models::DealPoolEntry>,
        qty: i32,
        reward_pool: Vec<models::DealPoolEntry>,
        sort: i32,
        updated_at: chrono::DateTime<chrono::FixedOffset>,
        windows: Vec<models::SaleWindow>,
    ) -> DealRule {
        DealRule {
            branch_overrides,
            created_at,
            get_percent: None,
            get_qty: None,
            id,
            is_active,
            kind,
            max_per_order: None,
            name,
            name_translations,
            pool,
            price: None,
            qty,
            reward_pool,
            sell: None,
            sort,
            updated_at,
            windows,
        }
    }
}
