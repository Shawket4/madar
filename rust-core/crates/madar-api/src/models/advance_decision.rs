/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// AdvanceDecision : Deciding a SALARY ADVANCE. Deliberately not called `DecisionRequest`: staff requests have their own decision body carrying `is_paid`, and two structs sharing a name collapse into one OpenAPI schema — which silently gave every generated client the wrong shape for one of them.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct AdvanceDecision {
    #[serde(rename = "note", default, with = "::serde_with::rust::double_option", skip_serializing_if = "Option::is_none")]
    pub note: Option<Option<String>>,
    /// `approved` | `rejected` | `cancelled`.
    #[serde(rename = "status")]
    pub status: String,
}

impl AdvanceDecision {
    /// Deciding a SALARY ADVANCE. Deliberately not called `DecisionRequest`: staff requests have their own decision body carrying `is_paid`, and two structs sharing a name collapse into one OpenAPI schema — which silently gave every generated client the wrong shape for one of them.
    pub fn new(status: String) -> AdvanceDecision {
        AdvanceDecision {
            note: None,
            status,
        }
    }
}

