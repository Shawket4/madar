/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// ScanResult : What the teller's scan screen shows.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct ScanResult {
    #[serde(rename = "member")]
    pub member: Box<models::MemberView>,
    /// Recent history, so a teller can answer \"where did my points go?\".
    #[serde(rename = "recent")]
    pub recent: Vec<models::LedgerEntry>,
    /// What this member could claim at this branch right now. Empty until the balance reaches the threshold, so the screen cannot tempt a teller into handing over a reward that has not been earned.
    #[serde(rename = "rewards")]
    pub rewards: Vec<models::RewardItem>,
}

impl ScanResult {
    /// What the teller's scan screen shows.
    pub fn new(member: models::MemberView, recent: Vec<models::LedgerEntry>, rewards: Vec<models::RewardItem>) -> ScanResult {
        ScanResult {
            member: Box::new(member),
            recent,
            rewards,
        }
    }
}

