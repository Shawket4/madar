/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// PastOrder : One past visit, as the customer's own page shows it.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct PastOrder {
    #[serde(rename = "branch_name")]
    pub branch_name: String,
    #[serde(rename = "id")]
    pub id: uuid::Uuid,
    /// What they had. The customer asked for this to be here; see the note on the handler about who else can see it.
    #[serde(rename = "items")]
    pub items: Vec<String>,
    #[serde(rename = "placed_at")]
    pub placed_at: chrono::DateTime<chrono::FixedOffset>,
    /// In piastres, like every other figure on the wire.
    #[serde(rename = "total")]
    pub total: i32,
}

impl PastOrder {
    /// One past visit, as the customer's own page shows it.
    pub fn new(
        branch_name: String,
        id: uuid::Uuid,
        items: Vec<String>,
        placed_at: chrono::DateTime<chrono::FixedOffset>,
        total: i32,
    ) -> PastOrder {
        PastOrder {
            branch_name,
            id,
            items,
            placed_at,
            total,
        }
    }
}
