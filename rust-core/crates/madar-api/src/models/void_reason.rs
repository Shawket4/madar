/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// VoidReason : Why a sale or a bill was torn up — the `void_reason` enum, shared by `orders` and `open_tickets` so a void-rate report reads counter and dine-in alike without a translation layer. Bound as text and cast in SQL (`$n::void_reason`), the way the order void has always done it.
/// Why a sale or a bill was torn up — the `void_reason` enum, shared by `orders` and `open_tickets` so a void-rate report reads counter and dine-in alike without a translation layer. Bound as text and cast in SQL (`$n::void_reason`), the way the order void has always done it.
#[derive(Clone, Copy, Debug, Eq, PartialEq, Ord, PartialOrd, Hash, Serialize, Deserialize)]
pub enum VoidReason {
    #[serde(rename = "customer_request")]
    CustomerRequest,
    #[serde(rename = "wrong_order")]
    WrongOrder,
    #[serde(rename = "quality_issue")]
    QualityIssue,
    #[serde(rename = "other")]
    Other,
}

impl std::fmt::Display for VoidReason {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        match self {
            Self::CustomerRequest => write!(f, "customer_request"),
            Self::WrongOrder => write!(f, "wrong_order"),
            Self::QualityIssue => write!(f, "quality_issue"),
            Self::Other => write!(f, "other"),
        }
    }
}

impl Default for VoidReason {
    fn default() -> VoidReason {
        Self::CustomerRequest
    }
}
