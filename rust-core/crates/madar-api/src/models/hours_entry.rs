/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// HoursEntry : One weekly booking window. `dow`: 0 = Sunday … 6 = Saturday. `open`/`close` are `HH:MM` local; a close at or before open means \"closes after midnight\".
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct HoursEntry {
    #[serde(rename = "close")]
    pub close: String,
    #[serde(rename = "dow")]
    pub dow: u32,
    #[serde(rename = "open")]
    pub open: String,
}

impl HoursEntry {
    /// One weekly booking window. `dow`: 0 = Sunday … 6 = Saturday. `open`/`close` are `HH:MM` local; a close at or before open means \"closes after midnight\".
    pub fn new(close: String, dow: u32, open: String) -> HoursEntry {
        HoursEntry { close, dow, open }
    }
}
