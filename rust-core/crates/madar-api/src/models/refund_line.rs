/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct RefundLine {
    #[serde(rename = "amount")]
    pub amount: i32,
    #[serde(rename = "id")]
    pub id: uuid::Uuid,
    /// `order_items.item_name`, so a receipt reprint names the dish without a second lookup.
    #[serde(rename = "item_name")]
    pub item_name: String,
    #[serde(rename = "order_item_id")]
    pub order_item_id: uuid::Uuid,
    #[serde(rename = "quantity")]
    pub quantity: i32,
    /// Whether the goods came back. Recorded per line; nothing in this module writes it `true` yet (see the module docs on restock).
    #[serde(rename = "restock")]
    pub restock: bool,
}

impl RefundLine {
    pub fn new(
        amount: i32,
        id: uuid::Uuid,
        item_name: String,
        order_item_id: uuid::Uuid,
        quantity: i32,
        restock: bool,
    ) -> RefundLine {
        RefundLine {
            amount,
            id,
            item_name,
            order_item_id,
            quantity,
            restock,
        }
    }
}
