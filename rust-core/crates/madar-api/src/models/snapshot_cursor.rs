/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// SnapshotCursor : Where a paged full snapshot stands. Every page reads the same horizon and window, so the pages together are ONE snapshot: a ledger row that changes while the pages are fetched moves past the horizon and arrives in the incremental pull that follows (`since = next`), never twice and never lost.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct SnapshotCursor {
    /// Ledger rows with `seq` above this come next.
    #[serde(rename = "after_seq")]
    pub after_seq: i64,
    #[serde(rename = "horizon")]
    pub horizon: i64,
    /// When the snapshot began (RFC 3339): a till that closes while the pages are fetched keeps its rows in the later pages.
    #[serde(rename = "started_at")]
    pub started_at: String,
    /// The ledger window start of this snapshot (RFC 3339).
    #[serde(rename = "window_from")]
    pub window_from: String,
}

impl SnapshotCursor {
    /// Where a paged full snapshot stands. Every page reads the same horizon and window, so the pages together are ONE snapshot: a ledger row that changes while the pages are fetched moves past the horizon and arrives in the incremental pull that follows (`since = next`), never twice and never lost.
    pub fn new(
        after_seq: i64,
        horizon: i64,
        started_at: String,
        window_from: String,
    ) -> SnapshotCursor {
        SnapshotCursor {
            after_seq,
            horizon,
            started_at,
            window_from,
        }
    }
}
