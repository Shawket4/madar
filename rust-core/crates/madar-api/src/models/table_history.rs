/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// TableHistory : What a table has done over the window asked for.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct TableHistory {
    /// Mean spend per settled bill, minor units.
    #[serde(rename = "average_bill_minor")]
    pub average_bill_minor: i64,
    /// Mean minutes a party occupied the table, over settled bills — the number that says whether a table turns.
    #[serde(rename = "average_minutes")]
    pub average_minutes: i64,
    /// Settled bills only.
    #[serde(rename = "covers")]
    pub covers: i64,
    #[serde(rename = "from")]
    pub from: chrono::DateTime<chrono::FixedOffset>,
    #[serde(rename = "label")]
    pub label: String,
    #[serde(rename = "settled_count")]
    pub settled_count: i64,
    /// Bills opened on this table in the window, newest first.
    #[serde(rename = "sittings")]
    pub sittings: Vec<models::TableSitting>,
    #[serde(rename = "table_id")]
    pub table_id: uuid::Uuid,
    #[serde(rename = "to")]
    pub to: chrono::DateTime<chrono::FixedOffset>,
    #[serde(rename = "total_minor")]
    pub total_minor: i64,
    /// Settled bills per day over the window, x100 so the wire stays integer.
    #[serde(rename = "turns_per_day_x100")]
    pub turns_per_day_x100: i64,
}

impl TableHistory {
    /// What a table has done over the window asked for.
    pub fn new(
        average_bill_minor: i64,
        average_minutes: i64,
        covers: i64,
        from: chrono::DateTime<chrono::FixedOffset>,
        label: String,
        settled_count: i64,
        sittings: Vec<models::TableSitting>,
        table_id: uuid::Uuid,
        to: chrono::DateTime<chrono::FixedOffset>,
        total_minor: i64,
        turns_per_day_x100: i64,
    ) -> TableHistory {
        TableHistory {
            average_bill_minor,
            average_minutes,
            covers,
            from,
            label,
            settled_count,
            sittings,
            table_id,
            to,
            total_minor,
            turns_per_day_x100,
        }
    }
}
