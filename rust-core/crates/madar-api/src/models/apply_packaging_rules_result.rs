/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct ApplyPackagingRulesResult {
    #[serde(rename = "catalog_revision")]
    pub catalog_revision: i64,
    /// Sizes (incl. linked copies) whose stored lines changed.
    #[serde(rename = "sizes_changed")]
    pub sizes_changed: i64,
    /// Sizes examined (every size of every live item in the org).
    #[serde(rename = "sizes_seen")]
    pub sizes_seen: i64,
    /// Sizes that still have an OWN line in a packaging category: typed by hand, they are kept (and win over a rule for the same ingredient) — review them.
    #[serde(rename = "sizes_with_manual_packaging")]
    pub sizes_with_manual_packaging: i64,
    /// Sizes that now carry at least one rule line.
    #[serde(rename = "sizes_with_rule")]
    pub sizes_with_rule: i64,
}

impl ApplyPackagingRulesResult {
    pub fn new(
        catalog_revision: i64,
        sizes_changed: i64,
        sizes_seen: i64,
        sizes_with_manual_packaging: i64,
        sizes_with_rule: i64,
    ) -> ApplyPackagingRulesResult {
        ApplyPackagingRulesResult {
            catalog_revision,
            sizes_changed,
            sizes_seen,
            sizes_with_manual_packaging,
            sizes_with_rule,
        }
    }
}
