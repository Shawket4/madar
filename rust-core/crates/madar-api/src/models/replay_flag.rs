/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// ReplayFlag : One offline act that was accepted despite failing the permission re-check (PERMISSIONS_ARCHITECTURE §4.4.5). The money already moved; this is the owner's notice, not a rollback.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct ReplayFlag {
    #[serde(rename = "author_id")]
    pub author_id: uuid::Uuid,
    #[serde(
        rename = "author_name",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub author_name: Option<Option<String>>,
    #[serde(
        rename = "branch_id",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub branch_id: Option<Option<uuid::Uuid>>,
    /// The `resource:action` cell the author did not hold.
    #[serde(rename = "capability")]
    pub capability: String,
    /// When it reached us. The gap is the offline window.
    #[serde(rename = "created_at")]
    pub created_at: chrono::DateTime<chrono::FixedOffset>,
    #[serde(rename = "id")]
    pub id: i64,
    /// When the act happened on the device.
    #[serde(rename = "occurred_at")]
    pub occurred_at: chrono::DateTime<chrono::FixedOffset>,
    /// The replayed op, e.g. `CashMovement`.
    #[serde(rename = "op")]
    pub op: String,
    /// `stale_snapshot` — they held it when they acted and the device had not heard the revocation yet. `unauthorized_offline` — nothing explains it.
    #[serde(rename = "reason")]
    pub reason: String,
    #[serde(
        rename = "reviewed_at",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub reviewed_at: Option<Option<chrono::DateTime<chrono::FixedOffset>>>,
    #[serde(
        rename = "reviewed_by",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub reviewed_by: Option<Option<uuid::Uuid>>,
}

impl ReplayFlag {
    /// One offline act that was accepted despite failing the permission re-check (PERMISSIONS_ARCHITECTURE §4.4.5). The money already moved; this is the owner's notice, not a rollback.
    pub fn new(
        author_id: uuid::Uuid,
        capability: String,
        created_at: chrono::DateTime<chrono::FixedOffset>,
        id: i64,
        occurred_at: chrono::DateTime<chrono::FixedOffset>,
        op: String,
        reason: String,
    ) -> ReplayFlag {
        ReplayFlag {
            author_id,
            author_name: None,
            branch_id: None,
            capability,
            created_at,
            id,
            occurred_at,
            op,
            reason,
            reviewed_at: None,
            reviewed_by: None,
        }
    }
}
