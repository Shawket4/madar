/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// DeliveryChannelSales : Delivery sales for one delivery channel (`in_mall` / `outside`). Revenue and order counts are over **delivered** orders only; `cancelled_orders` is shown separately so the UI can surface drop-off without inflating revenue.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct DeliveryChannelSales {
    #[serde(rename = "avg_order_value")]
    pub avg_order_value: i64,
    #[serde(rename = "cancelled_orders")]
    pub cancelled_orders: i64,
    /// Delivery channel: `in_mall` or `outside`.
    #[serde(rename = "channel")]
    pub channel: String,
    /// Sum of `delivery_fee` (piastres) over delivered orders.
    #[serde(rename = "delivery_fees")]
    pub delivery_fees: i64,
    #[serde(rename = "orders")]
    pub orders: i64,
    /// Sum of `total` (piastres) over delivered orders on this channel.
    #[serde(rename = "revenue")]
    pub revenue: i64,
}

impl DeliveryChannelSales {
    /// Delivery sales for one delivery channel (`in_mall` / `outside`). Revenue and order counts are over **delivered** orders only; `cancelled_orders` is shown separately so the UI can surface drop-off without inflating revenue.
    pub fn new(avg_order_value: i64, cancelled_orders: i64, channel: String, delivery_fees: i64, orders: i64, revenue: i64) -> DeliveryChannelSales {
        DeliveryChannelSales {
            avg_order_value,
            cancelled_orders,
            channel,
            delivery_fees,
            orders,
            revenue,
        }
    }
}

