/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// DeliveryChannelSales : Delivery sales for one delivery channel. Revenue and order counts are over **delivered** orders only; `cancelled_orders` is shown separately so the UI can surface drop-off without inflating revenue.  Read from `delivery_orders` — the quote — not from `orders`: a delivery that never settled into a sale still tells the shop something about its channels. Refunds are against the sale and are not netted here.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct DeliveryChannelSales {
    #[serde(rename = "avg_order_value")]
    pub avg_order_value: i64,
    #[serde(rename = "cancelled_orders")]
    pub cancelled_orders: i64,
    /// Delivery channel: `in_mall`, `outside`, `umbrella` or `pickup`.
    #[serde(rename = "channel")]
    pub channel: String,
    /// Sum of `delivery_fee` (piastres) over delivered orders. Outside the tax base and not food revenue; a pickup carries none.
    #[serde(rename = "delivery_fees")]
    pub delivery_fees: i64,
    /// `revenue − delivery_fees`: the bill for the goods (tax included), the figure comparable with dine-in and takeaway revenue.
    #[serde(rename = "goods_revenue", skip_serializing_if = "Option::is_none")]
    pub goods_revenue: Option<i64>,
    #[serde(rename = "orders")]
    pub orders: i64,
    /// Sum of `total` (piastres) over delivered orders on this channel — the whole bill, delivery fee included.
    #[serde(rename = "revenue")]
    pub revenue: i64,
}

impl DeliveryChannelSales {
    /// Delivery sales for one delivery channel. Revenue and order counts are over **delivered** orders only; `cancelled_orders` is shown separately so the UI can surface drop-off without inflating revenue.  Read from `delivery_orders` — the quote — not from `orders`: a delivery that never settled into a sale still tells the shop something about its channels. Refunds are against the sale and are not netted here.
    pub fn new(
        avg_order_value: i64,
        cancelled_orders: i64,
        channel: String,
        delivery_fees: i64,
        orders: i64,
        revenue: i64,
    ) -> DeliveryChannelSales {
        DeliveryChannelSales {
            avg_order_value,
            cancelled_orders,
            channel,
            delivery_fees,
            goods_revenue: None,
            orders,
            revenue,
        }
    }
}
