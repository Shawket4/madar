/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// LoyaltyAnalytics : The redemption report, computed in the database so a dashboard never loads every member to draw it.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct LoyaltyAnalytics {
    /// Balance earned, net of clawbacks written in the range.
    #[serde(rename = "earned_points")]
    pub earned_points: i64,
    #[serde(rename = "from")]
    pub from: chrono::DateTime<chrono::FixedOffset>,
    #[serde(rename = "liability")]
    pub liability: Box<models::PointsLiability>,
    /// Balance spent, net of reversals written in the range.
    #[serde(rename = "redeemed_points")]
    pub redeemed_points: i64,
    /// Units handed over as rewards.
    #[serde(rename = "redeemed_units")]
    pub redeemed_units: i64,
    /// Minor units of goods given away as rewards, on sales not voided.
    #[serde(rename = "redeemed_value_minor")]
    pub redeemed_value_minor: i64,
    /// Redemption rows in the range (one per covered order line).
    #[serde(rename = "redemptions")]
    pub redemptions: i64,
    /// Replayed sales whose rewards the points could not pay for.
    #[serde(rename = "refused_redemptions")]
    pub refused_redemptions: i64,
    #[serde(rename = "to")]
    pub to: chrono::DateTime<chrono::FixedOffset>,
    #[serde(rename = "top_rewards")]
    pub top_rewards: Vec<models::TopReward>,
}

impl LoyaltyAnalytics {
    /// The redemption report, computed in the database so a dashboard never loads every member to draw it.
    pub fn new(
        earned_points: i64,
        from: chrono::DateTime<chrono::FixedOffset>,
        liability: models::PointsLiability,
        redeemed_points: i64,
        redeemed_units: i64,
        redeemed_value_minor: i64,
        redemptions: i64,
        refused_redemptions: i64,
        to: chrono::DateTime<chrono::FixedOffset>,
        top_rewards: Vec<models::TopReward>,
    ) -> LoyaltyAnalytics {
        LoyaltyAnalytics {
            earned_points,
            from,
            liability: Box::new(liability),
            redeemed_points,
            redeemed_units,
            redeemed_value_minor,
            redemptions,
            refused_redemptions,
            to,
            top_rewards,
        }
    }
}
