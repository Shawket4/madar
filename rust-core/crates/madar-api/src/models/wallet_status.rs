/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct WalletStatus {
    /// Apple's push channel, which is SEPARATE from pass signing.  A pass can be issued perfectly and never change on anyone's phone, because the two are configured independently — and this panel used to report Apple as fine while every balance update went nowhere.
    #[serde(rename = "apns")]
    pub apns: Box<models::WalletProvider>,
    #[serde(rename = "apple")]
    pub apple: Box<models::WalletProvider>,
    #[serde(rename = "google")]
    pub google: Box<models::WalletProvider>,
}

impl WalletStatus {
    pub fn new(
        apns: models::WalletProvider,
        apple: models::WalletProvider,
        google: models::WalletProvider,
    ) -> WalletStatus {
        WalletStatus {
            apns: Box::new(apns),
            apple: Box::new(apple),
            google: Box::new(google),
        }
    }
}
