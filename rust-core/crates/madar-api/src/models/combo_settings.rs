/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// ComboSettings : `GET /settings/combos`: the minimum margin (C11) and the channel toggles (§11.1) that gate every combo and every deal.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct ComboSettings {
    /// Every branch that overrides at least one toggle.
    #[serde(rename = "branch_overrides")]
    pub branch_overrides: Vec<models::BranchChannelOverride>,
    /// The org-wide toggles.
    #[serde(rename = "channels")]
    pub channels: Box<models::ChannelToggles>,
    /// The owner's minimum margin as a decimal fraction string (\"0.5500\"); `null` = no margin warning.
    #[serde(
        rename = "min_margin",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub min_margin: Option<Option<String>>,
}

impl ComboSettings {
    /// `GET /settings/combos`: the minimum margin (C11) and the channel toggles (§11.1) that gate every combo and every deal.
    pub fn new(
        branch_overrides: Vec<models::BranchChannelOverride>,
        channels: models::ChannelToggles,
    ) -> ComboSettings {
        ComboSettings {
            branch_overrides,
            channels: Box::new(channels),
            min_margin: None,
        }
    }
}
