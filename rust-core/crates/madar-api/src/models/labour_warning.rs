/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// LabourWarning : A roster past a labour limit. Warns, never blocks (RU-13).
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct LabourWarning {
    /// The day (or, for a week's limit, the Saturday it starts).
    #[serde(rename = "date")]
    pub date: chrono::NaiveDate,
    #[serde(rename = "employee_id")]
    pub employee_id: uuid::Uuid,
    /// `day_hours` · `week_hours` · `presence` · `rest` · `weekly_rest` · `overtime_day`
    #[serde(rename = "kind")]
    pub kind: String,
    #[serde(rename = "limit_minutes")]
    pub limit_minutes: i64,
    #[serde(rename = "minutes")]
    pub minutes: i64,
}

impl LabourWarning {
    /// A roster past a labour limit. Warns, never blocks (RU-13).
    pub fn new(
        date: chrono::NaiveDate,
        employee_id: uuid::Uuid,
        kind: String,
        limit_minutes: i64,
        minutes: i64,
    ) -> LabourWarning {
        LabourWarning {
            date,
            employee_id,
            kind,
            limit_minutes,
            minutes,
        }
    }
}
