/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// LiabilityTrend : A weekly trend of the programme's liability, in the org's live currency (points or visits — never both; see [`PointsLiability`]).  `loyalty_customers.points_balance`/`visits_balance` are CURRENT balances with no history table, so this is not a snapshot of the outstanding balance at each week — it is each week's *net change* (earned minus redeemed, reversals netted in), read straight off the ledger. Summing `outstanding` across every week since the programme started would reconstruct the current balance; a single week says whether that week grew or shrank the liability.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct LiabilityTrend {
    #[serde(rename = "currency")]
    pub currency: String,
    #[serde(rename = "from")]
    pub from: chrono::DateTime<chrono::FixedOffset>,
    #[serde(rename = "points")]
    pub points: Vec<models::LiabilityTrendPoint>,
    #[serde(rename = "to")]
    pub to: chrono::DateTime<chrono::FixedOffset>,
}

impl LiabilityTrend {
    /// A weekly trend of the programme's liability, in the org's live currency (points or visits — never both; see [`PointsLiability`]).  `loyalty_customers.points_balance`/`visits_balance` are CURRENT balances with no history table, so this is not a snapshot of the outstanding balance at each week — it is each week's *net change* (earned minus redeemed, reversals netted in), read straight off the ledger. Summing `outstanding` across every week since the programme started would reconstruct the current balance; a single week says whether that week grew or shrank the liability.
    pub fn new(
        currency: String,
        from: chrono::DateTime<chrono::FixedOffset>,
        points: Vec<models::LiabilityTrendPoint>,
        to: chrono::DateTime<chrono::FixedOffset>,
    ) -> LiabilityTrend {
        LiabilityTrend {
            currency,
            from,
            points,
            to,
        }
    }
}
