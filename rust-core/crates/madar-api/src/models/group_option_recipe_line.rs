/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// GroupOptionRecipeLine : One recipe line of a modifier option, as the group editor shows it.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct GroupOptionRecipeLine {
    #[serde(rename = "ingredient_id")]
    pub ingredient_id: uuid::Uuid,
    #[serde(rename = "ingredient_name")]
    pub ingredient_name: String,
    #[serde(rename = "quantity")]
    pub quantity: f64,
    /// `null` = the generic line (every size); else the per-size amount for that size label (menu modeling B9). The editor must round-trip it on save.
    #[serde(
        rename = "size_label",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub size_label: Option<Option<String>>,
    #[serde(rename = "unit")]
    pub unit: String,
}

impl GroupOptionRecipeLine {
    /// One recipe line of a modifier option, as the group editor shows it.
    pub fn new(
        ingredient_id: uuid::Uuid,
        ingredient_name: String,
        quantity: f64,
        unit: String,
    ) -> GroupOptionRecipeLine {
        GroupOptionRecipeLine {
            ingredient_id,
            ingredient_name,
            quantity,
            size_label: None,
            unit,
        }
    }
}
