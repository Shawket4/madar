/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// RefundIssued : What `POST /refunds` returns: the refund, plus where the order now stands so the till can print \"fully refunded\" without a second request.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct RefundIssued {
    #[serde(rename = "amount")]
    pub amount: i32,
    #[serde(rename = "branch_id")]
    pub branch_id: uuid::Uuid,
    #[serde(rename = "client_ref", skip_serializing_if = "Option::is_none")]
    pub client_ref: Option<uuid::Uuid>,
    #[serde(rename = "created_at")]
    pub created_at: chrono::DateTime<chrono::FixedOffset>,
    #[serde(rename = "id")]
    pub id: uuid::Uuid,
    /// Whether `method` meant cash when the refund was issued. Snapshotted.
    #[serde(rename = "is_cash")]
    pub is_cash: bool,
    #[serde(rename = "issued_at")]
    pub issued_at: chrono::DateTime<chrono::FixedOffset>,
    #[serde(rename = "issued_by")]
    pub issued_by: uuid::Uuid,
    #[serde(rename = "issued_by_name")]
    pub issued_by_name: String,
    #[serde(rename = "method")]
    pub method: String,
    #[serde(rename = "note", skip_serializing_if = "Option::is_none")]
    pub note: Option<String>,
    #[serde(rename = "order_id")]
    pub order_id: uuid::Uuid,
    /// One of the [`RefundReason`] spellings.
    #[serde(rename = "reason")]
    pub reason: String,
    /// The shift the refund was ISSUED in — the drawer the money left. Not necessarily the shift the order was sold in.
    #[serde(rename = "shift_id")]
    pub shift_id: uuid::Uuid,
    #[serde(rename = "lines")]
    pub lines: Vec<models::RefundLine>,
    #[serde(rename = "refund_count")]
    pub refund_count: i64,
    #[serde(rename = "refunded_amount")]
    pub refunded_amount: i64,
    /// The cash slice of `refunded_amount` — what left a drawer.
    #[serde(rename = "refunded_cash")]
    pub refunded_cash: i64,
    /// `orders.status` after this refund — `refunded` only when the cumulative amount reached the total (the trigger's rule, not this module's).
    #[serde(rename = "order_status")]
    pub order_status: String,
    /// `total_amount − refunded_amount`: what may still be returned.
    #[serde(rename = "refundable_remaining")]
    pub refundable_remaining: i64,
}

impl RefundIssued {
    /// What `POST /refunds` returns: the refund, plus where the order now stands so the till can print \"fully refunded\" without a second request.
    pub fn new(
        amount: i32,
        branch_id: uuid::Uuid,
        created_at: chrono::DateTime<chrono::FixedOffset>,
        id: uuid::Uuid,
        is_cash: bool,
        issued_at: chrono::DateTime<chrono::FixedOffset>,
        issued_by: uuid::Uuid,
        issued_by_name: String,
        method: String,
        order_id: uuid::Uuid,
        reason: String,
        shift_id: uuid::Uuid,
        lines: Vec<models::RefundLine>,
        refund_count: i64,
        refunded_amount: i64,
        refunded_cash: i64,
        order_status: String,
        refundable_remaining: i64,
    ) -> RefundIssued {
        RefundIssued {
            amount,
            branch_id,
            client_ref: None,
            created_at,
            id,
            is_cash,
            issued_at,
            issued_by,
            issued_by_name,
            method,
            note: None,
            order_id,
            reason,
            shift_id,
            lines,
            refund_count,
            refunded_amount,
            refunded_cash,
            order_status,
            refundable_remaining,
        }
    }
}
