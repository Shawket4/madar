/*
 * Madar API
 *
 * Madar POS — multi-tenant cafe and restaurant management. The Rust backend is the source of truth for this spec; the Flutter teller app and React management dashboard consume the generated openapi.json.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct BranchFairness {
    #[serde(rename = "accepted")]
    pub accepted: i64,
    #[serde(rename = "branch_id")]
    pub branch_id: uuid::Uuid,
    #[serde(rename = "branch_name")]
    pub branch_name: String,
    /// Suggestions the gender default decided, of those decided in the month.
    #[serde(rename = "by_default_decided")]
    pub by_default_decided: i64,
    #[serde(rename = "decided")]
    pub decided: i64,
    /// Over 20 points (design §4.2).
    #[serde(rename = "flagged")]
    pub flagged: bool,
    /// The widest gap, in percentage points, between a gender's share of the night shifts and its share of stated willingness (of headcount when nobody stated any).
    #[serde(rename = "gap_points")]
    pub gap_points: i64,
    /// Learning is paused at this branch: under 40% accepted over 4 weeks.
    #[serde(rename = "learning_frozen")]
    pub learning_frozen: bool,
    #[serde(rename = "rows")]
    pub rows: Vec<models::FairnessRow>,
}

impl BranchFairness {
    pub fn new(
        accepted: i64,
        branch_id: uuid::Uuid,
        branch_name: String,
        by_default_decided: i64,
        decided: i64,
        flagged: bool,
        gap_points: i64,
        learning_frozen: bool,
        rows: Vec<models::FairnessRow>,
    ) -> BranchFairness {
        BranchFairness {
            accepted,
            branch_id,
            branch_name,
            by_default_decided,
            decided,
            flagged,
            gap_points,
            learning_frozen,
            rows,
        }
    }
}
