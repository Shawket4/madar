name: rust_bridge
description: Madar POS FFI bridge to madar-core via flutter_rust_bridge, plus domain repositories.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.11.0
  flutter: ">=3.41.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
