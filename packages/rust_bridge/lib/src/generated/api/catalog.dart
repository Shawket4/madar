// This file is automatically generated, so please do not edit it.
// @generated by `flutter_rust_bridge`@ 2.12.0.

// ignore_for_file: invalid_use_of_internal_member, unused_import, unnecessary_import

import '../frb_generated.dart';
import 'package:flutter_rust_bridge/flutter_rust_bridge_for_generated.dart';

/// One ingredient embedded in an addon item (`/addon-items` wire). Drives the
/// recipe preview: a milk/coffee addon's first ingredient replaces the base
/// line; other addons add their ingredients (scaled by qty).
class AddonIngredientView {
  final String ingredientName;
  final String unit;
  final double quantity;
  final String? orgIngredientId;

  const AddonIngredientView({
    required this.ingredientName,
    required this.unit,
    required this.quantity,
    this.orgIngredientId,
  });

  @override
  int get hashCode =>
      ingredientName.hashCode ^
      unit.hashCode ^
      quantity.hashCode ^
      orgIngredientId.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is AddonIngredientView &&
          runtimeType == other.runtimeType &&
          ingredientName == other.ingredientName &&
          unit == other.unit &&
          quantity == other.quantity &&
          orgIngredientId == other.orgIngredientId;
}

class AddonItemView {
  final String id;
  final String name;
  final String addonType;
  final PlatformInt64 defaultPriceMinor;
  final bool isActive;

  /// Embedded ingredient rows (recipe preview input). Empty when the addon has
  /// no stock impact (e.g. a flavour shot) or the wire omitted them.
  final List<AddonIngredientView> ingredients;

  const AddonItemView({
    required this.id,
    required this.name,
    required this.addonType,
    required this.defaultPriceMinor,
    required this.isActive,
    required this.ingredients,
  });

  @override
  int get hashCode =>
      id.hashCode ^
      name.hashCode ^
      addonType.hashCode ^
      defaultPriceMinor.hashCode ^
      isActive.hashCode ^
      ingredients.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is AddonItemView &&
          runtimeType == other.runtimeType &&
          id == other.id &&
          name == other.name &&
          addonType == other.addonType &&
          defaultPriceMinor == other.defaultPriceMinor &&
          isActive == other.isActive &&
          ingredients == other.ingredients;
}

class AddonSlotView {
  final String id;
  final String? label;
  final String addonType;
  final bool isRequired;
  final int minSelections;

  /// `None` ⇒ multi-select with no cap (R9).
  final int? maxSelections;

  const AddonSlotView({
    required this.id,
    this.label,
    required this.addonType,
    required this.isRequired,
    required this.minSelections,
    this.maxSelections,
  });

  @override
  int get hashCode =>
      id.hashCode ^
      label.hashCode ^
      addonType.hashCode ^
      isRequired.hashCode ^
      minSelections.hashCode ^
      maxSelections.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is AddonSlotView &&
          runtimeType == other.runtimeType &&
          id == other.id &&
          label == other.label &&
          addonType == other.addonType &&
          isRequired == other.isRequired &&
          minSelections == other.minSelections &&
          maxSelections == other.maxSelections;
}

/// One item that makes up a bundle (hydrated from the bundle list). The
/// component's base price is never charged separately — the bundle price covers
/// it; only its addon/optional up-charges add money.
class BundleComponentView {
  final String itemId;
  final String itemName;
  final PlatformInt64 quantity;

  const BundleComponentView({
    required this.itemId,
    required this.itemName,
    required this.quantity,
  });

  @override
  int get hashCode => itemId.hashCode ^ itemName.hashCode ^ quantity.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is BundleComponentView &&
          runtimeType == other.runtimeType &&
          itemId == other.itemId &&
          itemName == other.itemName &&
          quantity == other.quantity;
}

class BundleView {
  final String id;
  final String name;
  final String? description;
  final PlatformInt64 priceMinor;
  final String? imageUrl;

  /// `status == active`. The date/time availability window (below) is gated in
  /// the branch timezone by the cart/order context, not in this static read.
  final bool isAvailable;
  final String? availableFromDate;
  final String? availableUntilDate;
  final String? availableFromTime;
  final String? availableUntilTime;

  /// The bundle's component items (which menu item + how many). The detail
  /// sheet configures each one through the normal item-customization flow.
  final List<BundleComponentView> components;

  const BundleView({
    required this.id,
    required this.name,
    this.description,
    required this.priceMinor,
    this.imageUrl,
    required this.isAvailable,
    this.availableFromDate,
    this.availableUntilDate,
    this.availableFromTime,
    this.availableUntilTime,
    required this.components,
  });

  @override
  int get hashCode =>
      id.hashCode ^
      name.hashCode ^
      description.hashCode ^
      priceMinor.hashCode ^
      imageUrl.hashCode ^
      isAvailable.hashCode ^
      availableFromDate.hashCode ^
      availableUntilDate.hashCode ^
      availableFromTime.hashCode ^
      availableUntilTime.hashCode ^
      components.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is BundleView &&
          runtimeType == other.runtimeType &&
          id == other.id &&
          name == other.name &&
          description == other.description &&
          priceMinor == other.priceMinor &&
          imageUrl == other.imageUrl &&
          isAvailable == other.isAvailable &&
          availableFromDate == other.availableFromDate &&
          availableUntilDate == other.availableUntilDate &&
          availableFromTime == other.availableFromTime &&
          availableUntilTime == other.availableUntilTime &&
          components == other.components;
}

/// A resolved category style: an icon key + four hex colours (`#RRGGBB`).
class CatStyleView {
  /// Icon family key — the host maps it to a platform glyph. One of:
  /// coffee, mocha, bakery, lunch, icecream, drink, tea, water, ice, matcha, cafe.
  final String icon;
  final String bgTop;
  final String bgBottom;
  final String iconColor;
  final String accent;

  const CatStyleView({
    required this.icon,
    required this.bgTop,
    required this.bgBottom,
    required this.iconColor,
    required this.accent,
  });

  @override
  int get hashCode =>
      icon.hashCode ^
      bgTop.hashCode ^
      bgBottom.hashCode ^
      iconColor.hashCode ^
      accent.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is CatStyleView &&
          runtimeType == other.runtimeType &&
          icon == other.icon &&
          bgTop == other.bgTop &&
          bgBottom == other.bgBottom &&
          iconColor == other.iconColor &&
          accent == other.accent;
}

class CategoryView {
  final String id;
  final String name;
  final String? imageUrl;
  final bool isActive;

  const CategoryView({
    required this.id,
    required this.name,
    this.imageUrl,
    required this.isActive,
  });

  @override
  int get hashCode =>
      id.hashCode ^ name.hashCode ^ imageUrl.hashCode ^ isActive.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is CategoryView &&
          runtimeType == other.runtimeType &&
          id == other.id &&
          name == other.name &&
          imageUrl == other.imageUrl &&
          isActive == other.isActive;
}

class DiscountView {
  final String id;
  final String name;

  /// Open string: `percentage` | `fixed` | … — host interprets `value`.
  final String dtype;

  /// Percent points for `percentage`, minor-units for `fixed`.
  final PlatformInt64 value;
  final bool isActive;

  const DiscountView({
    required this.id,
    required this.name,
    required this.dtype,
    required this.value,
    required this.isActive,
  });

  @override
  int get hashCode =>
      id.hashCode ^
      name.hashCode ^
      dtype.hashCode ^
      value.hashCode ^
      isActive.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is DiscountView &&
          runtimeType == other.runtimeType &&
          id == other.id &&
          name == other.name &&
          dtype == other.dtype &&
          value == other.value &&
          isActive == other.isActive;
}

class ItemSizeView {
  final String id;
  final String label;

  /// Absolute price for this size (NOT a delta) — R9.
  final PlatformInt64 priceMinor;
  final bool isActive;

  const ItemSizeView({
    required this.id,
    required this.label,
    required this.priceMinor,
    required this.isActive,
  });

  @override
  int get hashCode =>
      id.hashCode ^ label.hashCode ^ priceMinor.hashCode ^ isActive.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is ItemSizeView &&
          runtimeType == other.runtimeType &&
          id == other.id &&
          label == other.label &&
          priceMinor == other.priceMinor &&
          isActive == other.isActive;
}

class MenuItemView {
  final String id;
  final String name;
  final String? description;
  final String? categoryId;
  final PlatformInt64 basePriceMinor;
  final String? imageUrl;
  final bool isActive;

  /// The item's default-milk addon (swap families charge only the delta over it).
  final String? defaultMilkAddonId;

  /// Per-item addon allowlist (ids). Non-empty ⇒ the sheet shows only these by
  /// default, with a "show all" escape hatch (mirrors the dashboard). Empty =
  /// no restriction (show the type's full set).
  final List<String> allowedAddonIds;
  final List<ItemSizeView> sizes;
  final List<AddonSlotView> addonSlots;
  final List<OptionalFieldView> optionalFields;

  /// The item's recipe lines (per size) — shown in the customization sheet.
  final List<RecipeLineView> recipes;

  const MenuItemView({
    required this.id,
    required this.name,
    this.description,
    this.categoryId,
    required this.basePriceMinor,
    this.imageUrl,
    required this.isActive,
    this.defaultMilkAddonId,
    required this.allowedAddonIds,
    required this.sizes,
    required this.addonSlots,
    required this.optionalFields,
    required this.recipes,
  });

  @override
  int get hashCode =>
      id.hashCode ^
      name.hashCode ^
      description.hashCode ^
      categoryId.hashCode ^
      basePriceMinor.hashCode ^
      imageUrl.hashCode ^
      isActive.hashCode ^
      defaultMilkAddonId.hashCode ^
      allowedAddonIds.hashCode ^
      sizes.hashCode ^
      addonSlots.hashCode ^
      optionalFields.hashCode ^
      recipes.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is MenuItemView &&
          runtimeType == other.runtimeType &&
          id == other.id &&
          name == other.name &&
          description == other.description &&
          categoryId == other.categoryId &&
          basePriceMinor == other.basePriceMinor &&
          imageUrl == other.imageUrl &&
          isActive == other.isActive &&
          defaultMilkAddonId == other.defaultMilkAddonId &&
          allowedAddonIds == other.allowedAddonIds &&
          sizes == other.sizes &&
          addonSlots == other.addonSlots &&
          optionalFields == other.optionalFields &&
          recipes == other.recipes;
}

class OptionalFieldView {
  final String id;
  final String name;
  final PlatformInt64 priceMinor;
  final bool isActive;

  /// Optional ingredient deduction: an optional that maps to stock carries a
  /// full `(name, unit, quantity)` triplet; cosmetic fields leave these `None`
  /// and contribute no recipe line. Mirrors Flutter's `OptionalField`.
  final String? ingredientName;
  final String? ingredientUnit;
  final double? quantityUsed;
  final String? orgIngredientId;

  const OptionalFieldView({
    required this.id,
    required this.name,
    required this.priceMinor,
    required this.isActive,
    this.ingredientName,
    this.ingredientUnit,
    this.quantityUsed,
    this.orgIngredientId,
  });

  @override
  int get hashCode =>
      id.hashCode ^
      name.hashCode ^
      priceMinor.hashCode ^
      isActive.hashCode ^
      ingredientName.hashCode ^
      ingredientUnit.hashCode ^
      quantityUsed.hashCode ^
      orgIngredientId.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is OptionalFieldView &&
          runtimeType == other.runtimeType &&
          id == other.id &&
          name == other.name &&
          priceMinor == other.priceMinor &&
          isActive == other.isActive &&
          ingredientName == other.ingredientName &&
          ingredientUnit == other.ingredientUnit &&
          quantityUsed == other.quantityUsed &&
          orgIngredientId == other.orgIngredientId;
}

class PaymentMethodView {
  final String id;
  final String name;
  final bool isCash;
  final String icon;
  final String color;

  const PaymentMethodView({
    required this.id,
    required this.name,
    required this.isCash,
    required this.icon,
    required this.color,
  });

  @override
  int get hashCode =>
      id.hashCode ^
      name.hashCode ^
      isCash.hashCode ^
      icon.hashCode ^
      color.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is PaymentMethodView &&
          runtimeType == other.runtimeType &&
          id == other.id &&
          name == other.name &&
          isCash == other.isCash &&
          icon == other.icon &&
          color == other.color;
}

class RecipeLineView {
  final String ingredientName;

  /// Quantity used (major units of the ingredient's unit, e.g. 18.0 g).
  final double quantity;
  final String unit;

  /// `None` = applies to all sizes; otherwise the size this line is for.
  final String? sizeLabel;

  /// Ingredient category (e.g. `milk`, `coffee_bean`) — the swap engine matches
  /// a milk/coffee addon against the base line of the same category.
  final String category;

  /// The org-ingredient identity — used to tell a real swap from re-selecting
  /// the default (same id ⇒ no swap). May be absent on older rows.
  final String? orgIngredientId;

  const RecipeLineView({
    required this.ingredientName,
    required this.quantity,
    required this.unit,
    this.sizeLabel,
    required this.category,
    this.orgIngredientId,
  });

  @override
  int get hashCode =>
      ingredientName.hashCode ^
      quantity.hashCode ^
      unit.hashCode ^
      sizeLabel.hashCode ^
      category.hashCode ^
      orgIngredientId.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is RecipeLineView &&
          runtimeType == other.runtimeType &&
          ingredientName == other.ingredientName &&
          quantity == other.quantity &&
          unit == other.unit &&
          sizeLabel == other.sizeLabel &&
          category == other.category &&
          orgIngredientId == other.orgIngredientId;
}
