name: feature_till
description: Madar POS open/close till, Z-report preview, cash movements, till history.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.11.0
  flutter: ">=3.41.0"

dependencies:
  app_core: ^0.1.0
  design_system: ^0.1.0
  feature_checkout: ^0.1.0
  feature_history: ^0.1.0
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.3.2
  rust_bridge: ^0.1.0

dev_dependencies:
  flutter_localizations:
    sdk: flutter
  flutter_test:
    sdk: flutter
