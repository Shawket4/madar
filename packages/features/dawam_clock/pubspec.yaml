name: feature_dawam_clock
description: "Dawam clock-in and timesheet: the geofenced punch, covers, tracking, corrections."
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.11.0
  flutter: ">=3.41.0"

dependencies:
  design_system: ^0.1.0
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.3.2
  staff_core: ^0.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
