name: feature_shift
description: Madar POS open/close shift, Z-report preview, cash movements, shift history.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.11.0
  flutter: ">=3.41.0"

dependencies:
  app_core: ^0.1.0
  design_system: ^0.1.0
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  rust_bridge: ^0.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
