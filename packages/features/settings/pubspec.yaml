name: feature_settings
description: Madar POS settings, sync center, diagnostics.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.11.0
  flutter: ">=3.41.0"

dependencies:
  design_system: ^0.1.0
  feature_checkout: ^0.1.0
  flutter:
    sdk: flutter
  rust_bridge: ^0.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
