name: staff_core
description: "Dawam's shared spine: the core handle, locale/theme/toast providers, the core's words, and the shared Dawam widgets. The staff app's app_core."
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.11.0
  flutter: ">=3.41.0"

dependencies:
  design_system: ^0.1.0
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
